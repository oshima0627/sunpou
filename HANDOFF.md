# HANDOFF

最終更新: **2026-08-24**

**このファイルだけ読めば再開できる**状態を保つこと。古くなった記述は消して書き直す。履歴は git log にある。

---

## いま何をしているのか

Amazon の商品比較サイト「**寸法で選ぶ**」を運営している。

**カタログに書かれていない寸法**（何本入るか・本当に収まるか）を、メーカー公表値から計算して答える記事を積む。
現在**記事2本**。Amazonアソシエイトの審査には**オリジナル記事10本**が要るので、あと8本。

| | |
|---|---|
| 公開URL | **https://sunpou.nexeed-lab.com/** |
| リポジトリ | `github.com/oshima0627/amazon-affiliate`（`main` が本番） |
| サイトのソース | `site/` |
| デプロイ | **`main` に push すると Cloudflare Workers Builds が自動でビルド＆デプロイ**。手動不要 |
| 運営者 | 大島 直孝 / info@nexeed-lab.com |

## 公開済みの記事

| URL | 内容 |
|---|---|
| `/coolerbox/500ml-honsuu/` | 500mlが何本入るか。**同じ20Lで15〜18本**。床面積が同じでも本数が違う |
| `/coolerbox/2l-tateru/` | 2Lは立つか。**8製品中1つだけ**。**1.5Lは2Lとほぼ同じ高さ** |

## 検証済みの事実（これを前提に動いてよい）

### 収益の前提

- Amazonの紹介料率は**2〜8%**。この価格帯（3,000〜15,000円）で**1件60〜960円**
- **勝てるロングテールは1記事あたり月500〜2,000円**（`docs/serp-check.md`。仮定を3つ含む試算）
- **月1万円 = 5〜20記事が1位を取れている状態**
- ⚠️ **リンククリック率と購入率が結論を丸ごと動かす。** 実測は自分の記事でしかできない。
  **最初の数記事の目的は収益ではなく、この2つを測ること**

### 記事の書き方を縛る制約

- ⚠️ **価格を手打ちで書けない。** Amazon規約上、価格表示はリンク／API経由のみ。
  PA-APIは売上が立つまで使えないので、**当面は価格に触れない構成**にする
- ⚠️ **クッキーは24時間**（カート投入で89日）。結論を記事後半に置くと取りこぼす
- **勝てるキーワードは2種類だけ**（`docs/serp-check.md`）
  1. **メーカーをまたぐ横断比較**（1社では書けない）
  2. **カタログに無い実測値・累計コスト**
  → サイズ・型番系はEC（楽天/Amazon/価格.com）が独占、規格の解説はメーカーが独占していて勝てない

### Amazonアソシエイト

- 審査＝**オリジナル記事10件以上＋直近60日以内の更新＋180日以内に3件の適格販売**
- **申請とリンク掲載は記事が揃う前でも可能**だが、**180日のタイマーは申請時点から動く**。
  → **記事が数本あって更新が続いている状態まで申請を待つ**
- ⚠️ **Amazon Content Partners（紹介料+1%）は米国限定で、日本からは登録できない**
  （`docs/affiliate-program-facts.md` の訂正を参照）

## 判断待ち（利用者に投げてある）

| # | 内容 |
|---|---|
| 1 | **Amazonbot を許可のまま維持するか戻すか。** +1%が取れない以上、いまは「対価なしでAmazonに学習を許している」状態。維持の理由は「Block AI bots が9月15日に廃止予定」「mixed purpose crawlers 問題の解消」「将来の拡大への先回り」の3つ |
| 2 | 記事03のテーマ（350ml缶か、別ジャンルか） |

## 未検証（`検証済み` に昇格させないこと）

- **350ml缶の寸法**。製缶メーカー3社（大和製罐・武内プレス・東洋製罐）を当たったが**寸法表が非公開**。
  「66φ×122mm」は二次情報にしかない。**これが取れないと記事03は書けない**
- 市販銘柄とボトル品名の対応（「い・ろ・は・す2L＝2L角ボトル(A)」等）
- 内寸の測り方がメーカー間で統一されているか
- 候補ジャンル15件のうち、クーラーボックス以外の検索需要（`docs/genre-candidates.md` は全件未検証）
- 消費者庁のステマ規制 運用基準PDF（未読）

## 触ってはいけないところ / 踏みやすい罠

- ⚠️ **日本語の太字。** 閉じの `**` が約物（`」。`）の直後にあり後ろに文字が続くと効かない。
  **ビルドが検出して落とす**（`assertNoRawEmphasis`）。書き方は `site/README.md`
- ⚠️ **SVGの文字の重なり。** 日本語は字面が広く、フォントサイズ分の行間では足りない。
  **目視ではなく `getBBox()` で交差判定する**（検査スクリプトは `site/README.md`）
- ⚠️ **`site/content/site.json` の `origin`。** canonical と sitemap がこの値を使う
- ⚠️ **`affiliateEnabled` を true にするとビルドが意図的に例外を投げる。**
  リンク解決の実装が先。フラグだけ立てても公開されない安全装置
- **原稿の正は `site/content/articles/`。** `drafts/` は使わない（2026-08-24 に廃止。二重管理で古い版が残っていた）
- `wrangler dev` を起動したまま新しい記事を足すと**アセットを拾わない**。dev を再起動する

## 次にやること

1. **記事を書く（あと8本）。** 同じクーラーボックスのデータで書けるものが残っている：
   - 350ml缶は何本（**缶の一次情報が取れたら**）
   - 何人分・何泊分（内寸から逆算）
2. **Google Search Console を見る。** sitemap 送信済み（6URL・成功）。順位が付き始めたら
   `docs/serp-check.md` の収益モデルを実測値で更新する
3. 判断待ち1・2 の回答を反映する

```bash
cd site && npm run dev      # http://localhost:8787
cd site && npm run build    # dist/ に出力
```

## ドキュメントの地図

| ファイル | 中身 |
|---|---|
| `docs/affiliate-program-facts.md` | 料率・審査・規約・PA-API・ステマ規制。**一次情報で確認済み** |
| `docs/serp-check.md` | 13キーワードのSERP目視／需要確認／**収益モデル** |
| `docs/genre-candidates.md` | 候補ジャンル15件（**全件未検証**） |
| `docs/article-01-…` `article-02-…` | 各記事のリサーチ記録と未検証事項 |
| `site/README.md` | サイトの実装。デザインの実測値、設定の理由、罠 |
