# HANDOFF

最終更新: **2026-09-16**（このファイルだけ読めば再開できる状態を保つ。**継ぎ足さず書き直す。履歴は git log。**）

---

## 1. いま何をしているのか

Amazon の商品比較サイト「**寸法帳**」（旧称「寸法で選ぶ」。2026-09 にブランド名だけ変更、URL・図版の署名は未変更）の運営。
**カタログに書かれていない寸法**を、メーカー公表値から計算して答える記事を積んでいる。
2026-08-25 に利用者から「運営を任せる」指示。手順の下敷きは `docs/operating-plan.md`。

| | |
|---|---|
| 公開URL | **https://sunpou.nexeed-lab.com/** |
| リポジトリ | `github.com/oshima0627/sunpou`（**`main` が本番**。push すると Cloudflare Workers Builds が2〜3分でデプロイ） |
| ソース | `site/`（Root directory は `/site`。これを指定しないと `package.json` を見つけられない） |
| Search Console | ドメインプロパティ `sc-domain:sunpou.nexeed-lab.com` |
| 運営者 | 大島 直孝 / info@nexeed-lab.com |
| 記事 | **23本**（クーラーボックス10／脚立・踏み台6／カセットコンロ5／**スーツケース2**） |
| X | **@sunpou_lab**（予約は **2026-09-18** まで。**フォロワー0・流入0**） |

## 2. ★ 状態：**表示は6日で17倍になった。売上はゼロ**（測定は 09-07 が最新。**09-16 時点で9日間測っていない → 次は `/measure`**）

`検証済み` — 根拠は **`docs/measurement-2026-09-07.md`**（GSCのデータは 08/23〜09/05。2日遅れ）

| | 8/31 | **9/07** |
|---|---|---|
| **GSC 表示回数（累計）** | 30 | **520** |
| **GSC クリック数（累計）** | 0 | **6**（初めて出た） |
| GSC 平均CTR | 0% | **1.2%**（＝実測値。仮定ではない） |
| GSC 平均掲載順位 | 13.4 | **9.6** |
| 表示のあったページ数 | 8 | **22** |
| **表示回数／日** | 3.7 | **80.6**（9/1〜9/5の平均。**21.8倍**） |
| インデックス | 19記事 | **27ページ・未登録0** |
| サイトマップ | 8/31・26ページ | **9/06 読込・成功・27ページ** |
| アソシエイトのクリック（30日） | 4 | **6**（うち9/1に2件＝作業日。**外部と切り分け不能**） |
| **注文・紹介料** | 0・¥0 | **0件・¥0** |

**8/30 → 8/31 で 14 → 73 に階段状に跳ね、以後6日間 71〜87 で高止まりしている。**
⚠️ **何が起きて跳ねたかは `未検証`**（同日に内部リンク修正・インデックス登録リクエストをしているが因果は未確認）。

| | |
|---|---|
| アソシエイトID | **`sunpou-22`**（2026-08-25 申請。支払い・税務情報も入力済み） |
| リンク | **398本すべて生きている**（`/s?k=<商品名>&tag=sunpou-22` の**検索リンク**・`rel="nofollow sponsored noopener"`） |
| 開示 | Amazon規約の文言＋景表法のPR表記、**両方とも全ページに出力済み** |
| 生成AI（AI Overview等）経由 | **累計1表示のみ**。⚠️ **「AIに食われている」説はこの数字では成り立たない** |

### ⚠️ 本審査は未達。**未達なのは「3件の適格販売」だけ**

`docs/shinsa-check-2026-08-25.md` で7要件を原文照合済み。記事の質・本数・更新頻度は**すべて充足**。

- **期限は 2027-02-21**（申請2026-08-25 + 180日）。**残り167日**
- **自己購入・家族への依頼はカウントされない**
- ⚠️ **「一度却下された申請を再度審査することはできません」（ヘルプ原文）。やり直しがきかない**

### 期限までの逆算：**「5〜16倍」から「約2倍」に縮んだ**

80.6表示/日 が続くと期限までに約13,460表示。⚠️ **「20%（記事→Amazon）」「5%（購入）」は `serp-check.md` の仮定のまま `未検証`。**

| サイトCTR | 期限までの適格販売 | 3件に必要な表示/日 | **現状(80.6)の何倍** |
|---|---|---|---|
| **1.2%（実測）** | **1.6件** | 150 | **1.9倍** |
| 2.0% | 2.7件 | 90 | 1.1倍 |
| 3.0% | 4.0件 | 60 | すでに足りている |

**表示回数を倍にするか、CTRを1.2%→2%に上げるか、どちらか一方で届く見込みになった。**

---

## 3. ★★ 次にやること（2026-09-07 の測定で 3-1 が引っくり返った）

根拠は **`docs/measurement-2026-09-07.md`**。

### 3-1. ★ **「資産の季節が逆」は、もう事実ではない**（前回の中心的な見立てが崩れた）

前回の HANDOFF は「表示のあった8ページ中7ページがクーラーボックス＝いちばん育っている資産が
これから需要1/5〜1/10になる側にある」と書いていた。**カテゴリ別の内訳が入れ替わった。**

| カテゴリ | 表示 | 割合 | クリック | 8/31時点 | トレンドの山 |
|---|---|---|---|---|---|
| **カセットコンロ** | **231** | **43.8%** | 0 | **0（1ページも出ていなかった）** | **12〜1月** |
| クーラーボックス | 184 | 34.8% | **6** | 30のうち29 | 7月（**冬は谷**） |
| 脚立・踏み台 | 112 | 21.2% | 0 | 30のうち1 | ほぼ平坦 |

**いちばん育っている資産は、いまカセットコンロ。これから需要が上がる側に来ている。**

→ **クーラーボックスの記事はこれ以上増やさない**（既存は来夏の資産として置く）。方針は変えない。
→ **次の記事は秋冬（土鍋・鍋まわり）**。方向は変わらないが、**「季節と逆行している」という焦りの前提は消えた。**

⚠️ **「秋冬に伸びる」はまだ `未検証`。答え合わせは10〜12月のGSCでしかできない。**

### 3-2. ★ **CTRが低いことが、初めて測定できるようになった**

上位3ページ（`cassette-konro/erabikata` 116表示・`kyatatsu/tenban-takasa` 77・`cassette-konro/bombe-honsuu` 70）は
**合計263表示・平均8.3位で、クリック0。**

| 仮定するCTR | クリック0になる確率 |
|---|---|
| 1.0% | 7.2% |
| **1.5%** | **1.9%** |
| 2.0% | 0.5% |

→ **8位前後でCTR 1%前後かそれ以下**と読むのが妥当。サイト全体の実測も **1.2%**。
**前回の逆算で使った 3%／5%／10% は、どれも実測より高すぎた。**

→ **`5-1`（タイトル短縮の保留）は解除してよい段階に来た。** ただし
⚠️ **20本を一斉に変えない。効果が測れなくなる。上位3ページだけ触って前後を比べる。**

⚠️ **留保**：最大クエリ（21表示）が「家庭で備えるカセットボンベの量について、1人分あたり1週間分の
目安として、最も適切なものを選んでください」という**設問文**で、クリックが起きない種類。CTRを下振れさせている。
また **520表示のうち431（83%）はまだ匿名化されていて、6クリックは全部その中で起きている。**

### 3-3. 秋冬の1本目は書いた（2026-09-07）。**ただしメーカー横断にはできていない**

**`/cassette-konro/donabe-ninzuu/`「土鍋の号数は何人前？」を公開した。** 詳細は `docs/article-21-donabe-ninzuu.md`。

銀峯陶器18製品の公表値から、**号数が決めているのは口径だけ**（号数内のばらつき最大1cm）で、
**容量は9号で1.45倍・目安人数は「2〜3人前」と「4〜5人前」**まで動くことを出した。
使える形として **必要な容量 ＝ 人数 × 0.7ℓ**（18製品すべてで公表レンジに0.5人以内で収まる）。

⚠️ **`serp-check.md` の勝てる型のうち、満たしたのは②「カタログに無い計算値」だけ。**
**①「メーカー横断」は満たしていない。** ニトリ・無印良品・佐治陶器ほかは 2026-09-07 時点で取得できず
（ニトリは実ブラウザでも JS が埋まらない／無印は拒否ページ／万古焼の各社はドメインが応答しない／
長谷園は「合」表記で号数ではない）。**他社が取れたら追記して初めて横断になる。**

**まだ残っている需要**（Googleサジェスト実測）：

> 土鍋 何号 サイズ／何号がいい／30cm 何号／24cm 何号／**3 4人用 何号**／測り方／内径／
> **早見表**／一覧／目安／比較／**9号 土鍋 何センチ**

⚠️ **`/cassette-konro/donabe-nangou/` の「扱っていないこと」に残る
「銀峯陶器以外の土鍋（未確認）」は、いまも穴のまま。** 新記事でも埋まっていない。

⚠️ **商品URLの推測は当たらない**（`hanamishima-donabe-8` は陶板が返った）。
**トップの `<a href>` から一覧を作り、Shopify の `/products/<id>.json` で取るのが速い**（`docs/article-21-…`）。

### 3-3b. スーツケース（`cabin-bag`）を2本書いた（2026-09-15・09-16）

紹介料率がサイト内で最も高いジャンル（`serp-check.md`：スーツケース960円）。カテゴリ表示名は「スーツケース」。

| 記事 | 型 | 根拠 |
|---|---|---|
| `/cabin-bag/airline-fit-outer-dims/` 機内持ち込みの一括判定（2026-09-15） | メーカー外寸 × 航空6社の規定 | `docs/article-22-airline-fit-outer-dims.md` |
| **`/cabin-bag/liter-nanpaku/` 40Lは何泊？（2026-09-16）** | **6社横断＋計算値**（ace・Legend Walker・サムソナイト・無印良品・ニトリ・RIMOWA の「容量↔泊数」目安を1表に。1泊あたりの容量を逆算、「10L×泊数＋10〜20L」で5社を説明。RIMOWA だけ外れる） | **`docs/article-23-liter-nanpaku.md`**／検算 `tools/check-article-23.py` |

`検証済み`（記事23）：`/article-check` の A1〜A4・B1〜B3 合格。`source-verifier` は ace・Legend Walker・サムソナイト一致、
無印・ニトリ・RIMOWA は verifier が取得不能 → **メインセッションが実ブラウザで文字列照合して全件一致**。

⚠️ **記事22は図版なし・`未検証` 項目多め**（`docs/article-22-…`）。
⚠️ **ace の `help_size.aspx` は「ペレット」「実容量」が HTML コメント内**。タグを剥がすだけの抽出だと非表示の文が混ざる。**`<!-- -->` を先に消す。**
⚠️ **サジェストに残っている需要**：`スーツケース 158cm 何リットル`／`3辺合計 158cm 何リットル`／`外寸 総外寸 違い`／`容量 計算`。
ace は製品ページに 本体サイズ・外寸・総外寸・容量 が揃うので取りやすい（`docs/article-23-…` §6）。

### 3-4. 表示0のページが3つある

`/coolerbox/horeizai-maisuu/`・`/kyatatsu/kaidan/`・**トップページ `/`**。
⚠️ **トップが1度も表示されていない理由は `未検証`。**

### 3-5. X の予約は **9/18 まで延ばした**（2026-09-07）

利用者の指示で3本を追加予約した。`検証済み`（Scheduled 一覧を読んで6本すべて確認）。

| 送信予定 | 記事 | カード |
|---|---|---|
| 9/08 21:00 | `/kyatatsu/omosa/` | kyatatsu-omosa.png |
| 9/10 21:00 | `/kyatatsu/secchi/` | kyatatsu-secchi.png |
| 9/12 21:00 | `/kyatatsu/erabikata/` | kyatatsu-erabikata.png |
| **9/14 21:00** | **`/cassette-konro/bombe-honsuu/`** | konro-bombe.png |
| **9/16 21:00** | **`/cassette-konro/nenshou-jikan/`** | konro-jikan.png |
| **9/18 21:00** | **`/cassette-konro/erabikata/`** | konro-erabikata.png |

**カセットコンロにしたのは 3-1 の理由**（表示回数の最大カテゴリ・需要の山が12〜1月）。
既存の予約9本中7本が脚立・踏み台に寄っていた。本文と ALT は `docs/x-drafts.txt` の [12]〜[14]。

⚠️ **次に切れるのは 9/18。**
⚠️ **フォロワー0・フォロー0のまま**（2026-09-07 に確認）。**graduated access で検索・トレンドに出にくい。
X からの流入は期待値ほぼ0**と見ておく。

---

## 4. 覚えておく運用ツール

```bash
node tools/sync-updated.mjs            # 差分を出すだけ（ずれていたら終了コード1）
node tools/sync-updated.mjs --write    # front matter を git の最終コミット日で書き換える
```
⚠️ **git の最終コミット日を見るので、記事を直したコミットの「次」に同期のコミットを置く。**

2026-09-01 に直した内容（図と本文の食い違い・製品カード・BreadcrumbList・キャッシュ・更新日のずれ）は
`docs/site-review-2026-09-01.md` と `-2.md` にある。**再開に必要な情報はもう残っていない。**

---

## 5. 意図的にやっていないこと（理由つき）

### 5-1. 記事タイトルの短縮（39〜56字。モバイルSERPは全角30字前後で切れる）

**2026-09-07 に保留の理由が消えた。** 測定が済み、**CTRが低いことが数字で出た**（3-2）。

⚠️ **ただし20本を一斉に変えない。** 変化がタイトルのせいか季節かを区別できなくなる。
**上位3ページ（`cassette-konro/erabikata`・`kyatatsu/tenban-takasa`・`cassette-konro/bombe-honsuu`）
だけを触り、他は対照群として残す。**

### 5-2. ASIN 直リンクへの差し替え（**398本すべてが検索リンク。直リンクは0本**）

検索リンクは `build.mjs:61-69` に意図した設計として理由がある（型番違い・生産終了で別物を指す事故を避ける）。
規約上も条件つきで可（「特別リンクと関連させた追加のオリジナルコンテンツ」＝当サイトは満たしている）。

⚠️ **ただし収益では、全リンクに1クリック分の段差を入れている。**
**アソシエイトのクリック総数が6件・外部由来と確認できたもの0件では、変えた効果を何とも比べられない。**
→ **流入が立ち上がってから、製品カード30枚だけを直リンクにして前後を比べる。**
`[[LINK:商品名|ASIN]]` の形を足せば記事の書き方は変えずに済む。**ASINは1件ずつ実際に開いて確かめる。**

### 5-3. カテゴリの並び替え（季節に合わせて カセットコンロ を先頭に）

**やっていない。** いま流入がほぼ0で、**変えても効果を測れない**。5-2 と同じ理由。
**秋冬の記事が実際に増えて流入が立ってから並べ替える。**

### 5-4. 「クールラインα3」か「クールライン α III」か

公式表記は α III、サイトは全記事 α3。**判断待ち。直すならサイト全体で一斉に。**

---

## 6. 検証済みの事実（これを前提に動いてよい）

### Amazonアソシエイト（`docs/affiliate-program-facts.md` / `shinsa-check-2026-08-25.md`）

- 審査＝**オリジナル記事10件以上＋直近60日以内の更新＋申請から180日以内に3件の適格販売**
- **却下されたら再審査はない**
- **検索結果へのリンクは条件つきで可**。条件は「特別リンクと関連させた追加のオリジナルコンテンツ」
- **申請時点でリンクの掲載が可能**になり、**180日のタイマーも申請時点から動く**
- **自己購入・家族への依頼はカウント外**。**媒体をまたいで合算される**
- **リンク経由なら紹介した商品以外が買われても報酬になる**
- **クッキーは24時間**（カート投入で89日）→ 結論とリンクを記事前半に置く
- ⚠️ **価格・在庫・商品画像は使えない。** Creators API 経由のみで、その API は
  **「過去30日間に適格販売10件」**が条件。**審査の「180日で3件」より厳しい**。PA-API v5 は 2026-05-15 終了済み
- ⚠️ **2026-04-20 施行の規約改定：有料広告・ブースト広告経由は不適格販売。広告で流入を買う手は使えない**
- ⚠️ **Amazon Content Partners（+1%）は米国限定**
- **SiteStripe は URLコピーだけ**（画像リンク・ウィジェットの選択肢が無い）

### 収益の前提（`serp-check.md`）

- 紹介料率2〜8%。**クーラーボックス240円／カセットコンロ120円（最下位）／スーツケース960円**
- ⚠️ **報酬額とSERPの勝ち目は逆相関している。** 単価だけで絞ると間違える
- **審査に要るのは「販売3件」であって金額ではない。** 冬に売れる定番品が最短距離

### 勝てるキーワードは2種類だけ（13KWのSERP目視から。`serp-check.md`）

1. **メーカーをまたぐ横断比較**（1社では書けない）
2. **カタログに無い実測値・累計コスト**

→ サイズ・型番系はEC（楽天・Amazon・価格.com）が独占、規格の解説はメーカー・公的機関が独占。**勝てない。**

⚠️ **実データで2回裏づけ済み**（2026-08-31・2026-09-07）：「カタログにない数値」型のクエリ
（`6尺は何cm` 7.3位／`カセットコンロ 鍋の大きさ` 8.2位／`クーラーボックス 数え方` 9.7位）が1ページ目。
**型番・一覧系は時間が経っても上がらず、むしろ下がった**（`フィクセル 17l` 8/31は34.3位 → 9/07は43位、
`クーラーボックス サイズ 一覧` 71位）。**方向は当たっている。量が足りない。**

### テーマを決めるときの手順

1. **一次情報が取れるかを先に確かめる**（350ml缶はこれで落ちた）
2. **Googleサジェストで需要を見る**（`suggestqueries.google.com/complete/search?client=chrome&hl=ja&gl=jp`）
3. **寸法で答えられない部分は記事の冒頭で明示して切る**
4. **季節を見る**（Googleトレンド。2026-09-01 に追加した観点）

### ジャンル固有の事実

記事ごとの根拠と全寸法は `docs/article-*.md` にある。**ここには判断に効くものだけ置く。**

- **ダイワ「KEEP」とシマノ「COOL」は同じ物差し**（JIS S 2048:2006 簡便法）。**比較してよい**。
  ⚠️ **アイリスオーヤマの「保冷日数」は別物差し。換算できない**。両社とも「目安であり保証値ではない」＝**優劣は書かない**
- **ダイワの内寸は「一番狭い所」、外寸は「一番広い所」。表記順は 縦（奥行）×幅×高さ**
- **シマノは内寸を「中央値」と「底部」で公表**（底部は中央値の93〜95%）。**底部を使う**
- **専用脚立は「型番の数字＝天板高さ」。はしご兼用は型番より9〜12cm低い**
- **踏台と脚立の境目は天板高さ80cm**（上わく付きなら80cm以上でも踏台）
- **五徳の径・土鍋の鍋底の径は、全メーカー非公表**

---

## 7. 判断待ち（利用者に投げてある）

| # | 内容 |
|---|---|
| 1 | **Amazonbot を許可のまま維持するか。** +1%（Content Partners）が日本から取れない以上、いまは「対価なしで学習を許している」状態 |
| 2 | **保冷力だけを扱う記事を作るか。** 事実としては書けるが**サイト名「寸法で選ぶ」の射程外** |
| 3 | **`/coolerbox/daiwa-shimano/` は「どちらが上とは書きません」と本文に明記している。** 製品カードを置いた（17.0%）が、立場と矛盾しないか |
| 4 | ~~X の予約が 9/12 で切れる~~ → **2026-09-07 に 9/18 まで延ばした**（3-5）。**次は 9/18 で切れる** |
| 5 | **★ 審査期限（2027-02-21）に間に合わない可能性をどう扱うか。** 広告は規約で使えず、**2026-09-07 時点で不足はおよそ2倍**（8/31時点の「5〜16倍」から縮んだ）。**落ちたら再申請できない。** 間に合わない見込みが強まったときに何をするかを決めておく必要がある |

---

## 8. 未検証（`検証済み` に昇格させないこと）

- **★ 2026-08-31 に表示回数が 14 → 73 へ跳ねた原因。** 同日にやったこと（内部リンクの修正・
  インデックス登録のリクエスト・記事19の登録）と時期は重なるが、**因果は確かめていない。**
  新記事がインデックスされて評価が始まっただけ、という説明も同じくらいありうる
- **★ 80.6表示/日 が続くか。** 6日ぶんの観測しかない。逆算の全部がこの数字に乗っている
- **★ 520表示のうち431（83%）のクエリ。** 匿名化されていて見えない。**GSCのクリック6件は全部この中で起きている**
- **★ トップページ `/` が1度も表示されていない理由**
- **秋冬に書けば審査に受かるか。** 季節の向きは Google トレンドで確認し、カセットコンロが最大カテゴリに
  なったのも事実だが、**効果は10〜12月のGSCでしか分からない**
- **350ml缶の寸法**（＝「クーラーボックス 缶 何本」は書けない。需要は最大だが一次情報が無い。2026-09-01 に再確認して同じ結論）
- **土鍋の号数が銀峯陶器以外でも不統一か**（2026-09-07 に再挑戦。ニトリは実ブラウザでも JS が埋まらず、
  無印良品は拒否ページ、万古焼の各社はドメインが応答しない。**取得できていない**）
- **銀峯陶器の公表「高さ」がフタを含むか／「容量」が満水か**（18製品どのページにも書かれていない）
- **アソシエイトのクリック6件が外部の人によるものか**（Cloudflareの訪問は全部 direct で切り分け不能。
  ⚠️ **うち2件は 9/1＝こちらが作業していた日に起きている**）
- **記事→Amazonのクリック率20%・購入率5%**（`serp-check.md` の仮定のまま。実測に置き換えられていない）
- **X で数字ネタが伸びるか。** ⚠️ **新規アカウントは graduated access で「検索やトレンドに出にくい」と X 自身が明記**。
  開設直後に伸びなくても、それだけでは何も分からない
- 候補ジャンル15件の検索需要（`docs/genre-candidates.md` は全件未検証）
- 記事固有の未検証は各 `docs/article-*.md` に置いてある

---

## 9. 踏みやすい罠

### 事実の扱い

- ⚠️ **WebFetch・WebSearch の要約に数値も引用文も読ませない。** JISのページで**HTMLに存在しない数値を返した**。
  記事13では**「アルインコも踏台の定義を書いている」と返したが自社サイトのどこにも無かった**。
  **数値と引用文は curl か browser で取って自分で grep する**
- ⚠️ **403 を「取得できない」と決めつけない。実ブラウザで開き直す**（アイリスオーヤマで実際に取れた）
- ⚠️ **curl が 403 のメーカー**：シマノ・アイリスオーヤマ・岩谷産業。**無印良品・ニトリは curl がタイムアウト**
- ⚠️ **イワタニは `navigate` で直接URLを開くとトップに飛ぶ。** タブを開いてから**ページ内の同期XHR**で取る。
  **極・アモルフォの仕様表は製品ページに無く `/function/` にある**
- ⚠️ **1ページに複数商品の仕様表が載っていることがある**（イワタニのミニのページに併売の鍋の表）
- ⚠️ **HTML コメント内の文を「公表」と読まない**（ace の容量の測り方は `<!-- -->` の中だった。2026-09-16 に verifier が見つけた）
- ⚠️ **curl が使えないメーカー（追加）**：RIMOWA は 403、無印良品・ニトリはタイムアウト／JS 描画。**実ブラウザで開き、同一オリジンの `fetch` で他ページを取る**と速い
- ⚠️ **商品ページのURLを推測しない**（`hanamishima-donabe-8` は陶板が返った）
- ⚠️ **メーカーの公表値にも表記ミスがある**（アイリスは20Lの外寸を「33.」と桁欠け）。**別ページを当たる**
- ⚠️ **「このメーカーは公表していない」はシリーズの話か会社の話かを分ける**（記事11で誤った）
- ⚠️ **2製品で確かめて一般化しない**（記事18が9機種中2機種で「火力が強いほど燃焼時間が短い」と書き、同じ表に反例があった）
- ⚠️ **数値を書いたら必ずスクリプトで再計算して突き合わせる**（記事17の「4.1本」は正解4.0本。検算だけが見つけた）
- ⚠️ **e-Gov 法令API v2 は本文を base64 の XML で返す**（`docs/article-12-kaidan.md` にコマンド）

### 記事と図版

- ⚠️ **記事の数字を変えたら、同じ数字を出している図と、他記事の相互参照も一緒に直す。**
  26→33製品のとき、**図と他8記事が取り残されていた**
- ⚠️ **図版のビルド検査は4つ**（`tools/figures/build.py`）：①図の「数値＋単位」がその図を使う記事の本文にあるか
  ②記事の alt の数値が図に出ているか ③見出しが枠に収まるか ④文字の重なり・はみ出し
- ⚠️ **数値の照合で「末尾の0を落とした形」を認めない。** 図の `3.0倍` を本文の `3倍` で通したら、
  無関係な「奥行は3倍以上」に当たって合格し、**3.0倍 vs 2.2倍 の食い違いを見逃した**
- ⚠️ **図版は PowerPoint で作る。SVG を手書きしない。PNG を直接いじらない。**
  正は `tools/figures/figures.py`。手直しは pptx → `python tools/figures/build.py --from-pptx`
- ⚠️ **画像に1日のキャッシュが付いている。図版の数値を直しても最大1日は古いものが出る**
- ⚠️ **日本語の太字。** 閉じの `**` が約物（`」。）`）の直後で後ろに文字が続くと効かない。**ビルドが落とす**
- ⚠️ **`build.mjs` は front matter の `eyecatch` を必ず本文の先頭に出す。** 本文にも同じ画像を書くと**2回出る**
- ⚠️ **リンク位置は `tools/check-link-position.py` で測る**（目次を含めたまま測ると数ポイント大きく出る）
- ⚠️ **煽り文句（数量限定・在庫僅少）は使わない**（景表法）。**価格・在庫に触れない**（Amazon規約）

### 環境

- ⚠️ **bash の出力に出た日本語をそのままクエリや原稿に使わない。** 端末が cp932 で化ける
  （`CS＋` → `CS白`。**化けた商品名でAmazonを引いて誤った結論を出したことがある**）。
  **日本語はファイルに UTF-8 で書き出して Read ツールで読む**（Python なら `io.open(..., encoding='utf-8')`）
- ⚠️ **heredoc で長い日本語Markdownを書くとクォートやバックスラッシュで落ちる。Write ツールを使う**
- ⚠️ **`site/public/` を直しても dev サーバは `site/dist/` の古いコピーを返す。`node site/build.mjs` を通す**
- ⚠️ **ワークツリーで始めたら `cd site && npm install`**（`node_modules` は共有されない）
- ⚠️ **`site/content/site.json` の `origin` を canonical と sitemap が使う**
- ⚠️ **GSCの「インデックス登録をリクエスト」は1日10件で打ち止め**。ドメインプロパティのサイトマップ欄は**フルURL**
- ⚠️ **X の投稿画面は1回目の `type` が飲まれる。ALT保存でモーダルの高さが変わり、古い座標を押すと書きかけが消える**
- ⚠️ **リベのノウハウ図書館記事のURLを公開記事に貼らない**（レビュー欄で禁止）
- **原稿の正は `site/content/articles/`。別の場所に下書きを置かない**

```bash
cd site && npm run dev      # http://localhost:8787
cd site && npm run build    # dist/ に出力
```

---

## 10. ドキュメントの地図

| ファイル | 中身 |
|---|---|
| `docs/site-review-2026-09-01-2.md` | 収益の観点で並べ直したレビュー。土鍋の需要／検索リンクの是非／更新日のずれ。⚠️ **「季節のずれ」の節は `measurement-2026-09-07.md` で覆っている** |
| `docs/site-review-2026-09-01.md` | 同日午前のレビュー（作りの穴）。前半＝見つけたもの、後半＝直した記録 |
| `docs/operating-plan.md` | 運営手順。リベ記事のSTEP1〜5／公式との差分／作業分担／申請の実務 |
| `docs/shinsa-check-2026-08-25.md` | **審査要件の原文照合。** 検索リンクの規約上の扱い／Xが審査対象外である確認 |
| `docs/affiliate-program-facts.md` | 料率・審査・規約・API・ステマ規制。**一次情報で確認済み** |
| `docs/serp-check.md` | 13キーワードのSERP目視／**勝てるKWの2類型**／収益モデル |
| `docs/link-check-2026-08-31.md` | **リンクの3層（出力・計測・着地）を端から端まで確認した回。** 着地19語の実測つき |
| `docs/link-appearance-2026-08-26.md` | Amazonリンクの見え方。画像・価格が使えない規約上の根拠 |
| **`docs/measurement-2026-09-07.md`** | **★ 実測の最新。** 表示が6日で17倍／**カテゴリ内訳の逆転**／初クリック／**CTR 1.2%の実測**／逆算は約2倍 |
| `docs/measurement-2026-08-31.md` | 前回。順位は問題ではなく律速は表示回数／逆算（5〜16倍・**9/07に更新された**）／**技術監査は全部シロ** |
| `docs/measurement-2026-08-29.md` / `-08-26.md` / `-08-25.md` | それ以前の測定。GSCの罠 |
| `docs/monetization-review.md` | 収益設計の診断 |
| `docs/sns-plan.md` | SNSの選定と X の運用。⚠️ Amazon公式ヘルプ2ページの記述の食い違いも記録 |
| `docs/genre-candidates.md` | 候補ジャンル15件（**全件未検証**） |
| `docs/article-*.md` | **記事ごとのデータ源・検算・未検証事項。** 数字の出どころを追うならここ |
| `docs/article-21-donabe-ninzuu.md` | 記事21（2026-09-07）。銀峯陶器18製品の取り方／検算33件／他社が取れなかった記録 |
| `docs/article-22-airline-fit-outer-dims.md` | 記事22（2026-09-15）。航空6社の規定と製品5つの外寸／○×の再計算 |
| **`docs/article-23-liter-nanpaku.md`** | **最新の記事（2026-09-16）。** 6社の容量↔泊数の出典と原文／取れなかった社／`/article-check` の記録（ace の HTML コメントの罠） |
| `docs/x-drafts.txt` | Xの投稿下書き（投稿済み／未投稿） |
| `site/README.md` | サイトの実装。デザインの実測値・設定の理由・罠 |
| `specs/article-contract.md` | **完了基準（二値）。1件でも不合格なら公開しない** |
| `specs/article-rubric.md` | 段階評価。**落第の判定には使わない** |

**Skill**：`/article-check`（書いた直後）／`/measure`（数日おき）／`/link-check`／`/fix-site`
