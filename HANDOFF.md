# HANDOFF

最終更新: **2026-08-24**

**このファイルだけ読めば再開できる**状態を保つこと。古くなった記述は消して書き直す。履歴は git log にある。

---

## いま何をしているのか

Amazon の商品比較サイト「**寸法で選ぶ**」を運営している。

**カタログに書かれていない寸法**（何本入るか・本当に収まるか）を、メーカー公表値から計算して答える記事を積む。
現在**記事7本**。Amazonアソシエイトの審査には**オリジナル記事10本**が要るので、あと3本。
**2026-08-25 に2つ目のカテゴリ「カセットコンロ」を追加した**（利用者の判断）。

| | |
|---|---|
| 公開URL | **https://sunpou.nexeed-lab.com/** |
| リポジトリ | `github.com/oshima0627/amazon-affiliate`（`main` が本番） |
| サイトのソース | `site/` |
| デプロイ | **`main` に push すると Cloudflare Workers Builds が自動でビルド＆デプロイ**。手動不要 |
| 運営者 | 大島 直孝 / info@nexeed-lab.com |

## 公開済みの記事

| URL | 内容 |
|---|---|
| `/coolerbox/500ml-honsuu/` | 500mlが何本入るか。**同じ20Lで15〜18本**。床面積が同じでも本数が違う |
| `/coolerbox/2l-tateru/` | 2Lは立つか。**8製品中1つだけ**。**1.5Lは2Lとほぼ同じ高さ** |
| `/coolerbox/horeizai-honsuu/` | 保冷剤で何本減るか。**同じ20Lに1枚入れて6本と18本**。置き方で決まる |
| `/coolerbox/yoryo-uchinori/` | 容量Lの正体。**外寸の体積の28.9〜40.0%しか中に使えない**。保冷日数は容量の4割を氷にした数字 |
| `/coolerbox/daiwa-shimano/` | ダイワ vs シマノ。**KEEPとCOOLは同じJIS簡便法の「時間」で比較できる**。同じボトルなら20Lと22Lは同じ15本 |
| `/coolerbox/uchinori-nagasa/` | 大型15製品を内寸の長辺順に。**80Lと60Lは長辺が同じ85cm**。斜め置きで4〜8cm、立体の対角で8〜13cm伸びる |
| `/cassette-konro/donabe-nangou/` | **カセットコンロと土鍋の号数。** 同じ9号でも幅31〜32.5cm。「10号まで」の機種の本体幅33.4cmより10号土鍋（34〜36cm）が大きい |

## 検証済みの事実（これを前提に動いてよい）

### 収益の前提

- Amazonの紹介料率は**2〜8%**。この価格帯（3,000〜15,000円）で**1件60〜960円**
- **勝てるロングテールは1記事あたり月500〜2,000円**（`docs/serp-check.md`。仮定を3つ含む試算）
- **月1万円 = 5〜20記事が1位を取れている状態**
- ⚠️ **リンククリック率と購入率が結論を丸ごと動かす。** 実測は自分の記事でしかできない。
  **最初の数記事の目的は収益ではなく、この2つを測ること**

### 記事の書き方を縛る制約

- ⚠️ **価格を手打ちで書けない。** Amazon規約上、価格表示はリンク／API経由のみ。
  PA-APIは売上が立つまで使えないので、**当面は価格に触れない構成**にする
- ⚠️ **クッキーは24時間**（カート投入で89日）。結論を記事後半に置くと取りこぼす
- **勝てるキーワードは2種類だけ**（`docs/serp-check.md`）
  1. **メーカーをまたぐ横断比較**（1社では書けない）
  2. **カタログに無い実測値・累計コスト**
  → サイズ・型番系はEC（楽天/Amazon/価格.com）が独占、規格の解説はメーカーが独占していて勝てない

### テーマを決めるときの手順（記事03で確立）

1. **一次情報が取れるかを先に確かめる。** 取れないテーマは書けない（350ml缶がこれで落ちた）
2. **Googleサジェストで需要を見る**（`/complete/search`, hl=ja, gl=jp を curl で引く）。
   aramakijake の「0」はデータ欠損なので使わない
3. **検索意図のうち寸法で答えられない部分を、記事の冒頭で明示して切る**
   （保冷剤の記事なら「何時間もつか」は扱わないと書く）

### 保冷力の表示（記事05で確定）

- **ダイワ「KEEP」とシマノ「COOL」は同じ物差し。** どちらも **JIS S 2048：2006 の簡便法**
  （外気40℃／本体容量の25%の角氷／8時間後の残存率）で、**氷が溶け切るまでの時間**に換算した値。
  **並べて比較してよい。** 両社とも「目安であり保証値ではない」と明記している
- ⚠️ **アイリスオーヤマの「保冷日数」は別物差し**（容量比40%の氷・30℃9h/20℃15h）。**換算できない**
- ⚠️ ロゴスは「氷点下パック併用でアイスクリーム最大11時間」。これも別物差し

### Amazonアソシエイト

- 審査＝**オリジナル記事10件以上＋直近60日以内の更新＋180日以内に3件の適格販売**
- **申請とリンク掲載は記事が揃う前でも可能**だが、**180日のタイマーは申請時点から動く**。
  → **記事が数本あって更新が続いている状態まで申請を待つ**
- ⚠️ **Amazon Content Partners（紹介料+1%）は米国限定で、日本からは登録できない**
  （`docs/affiliate-program-facts.md` の訂正を参照）

### カテゴリを2件にしたことで自動的に変わったこと（2026-08-25・実際に確認済み）

`site.json` の `categories` を2件にしたら、`showCategoryNav` が有効になって**設計どおり自動で切り替わった。**

| 対象 | 変化 |
|---|---|
| ヘッダー／フッターのナビ | カテゴリ2件が**出るようになった**（モバイル高さは84pxのまま） |
| `/coolerbox/` `/cassette-konro/` | **noindex が外れた**。sitemap にも載るようになった（現在13URL） |
| 関連記事 | ⚠️ **カテゴリ非対応だったので手を入れた。** 同じカテゴリを優先し、足りない分だけ他カテゴリで埋める（`build.mjs`） |

## 判断待ち（利用者に投げてある）

| # | 内容 |
|---|---|
| 1 | **Amazonbot を許可のまま維持するか戻すか。** +1%が取れない以上、いまは「対価なしでAmazonに学習を許している」状態。維持の理由は「Block AI bots が9月15日に廃止予定」「mixed purpose crawlers 問題の解消」「将来の拡大への先回り」の3つ |
| 2 | **保冷力だけを扱う記事（ランキング等）を作るか。** 事実の面は前進した（下記のとおり **KEEPとCOOLは同じ物差し**と判明し、記事05に事実として書いた）。**残っているのは「保冷力ランキングを独立記事にするか」の判断で、これはサイト名「寸法で選ぶ」の射程外。** 別サイトにするか射程を広げるかの判断が要る |

~~記事03のテーマ~~ → **決着した。** 350ml缶は**一次情報が無くて書けない**ことを確認し（下記）、
保冷剤の記事に差し替えて公開済み。経緯は `docs/article-03-coolerbox-horeizai.md`

## 未検証（`検証済み` に昇格させないこと）

- **350ml缶の寸法。書けない状態が続いている。** 2026-08-24 に再調査した結果：
  - [東洋製罐 DI缶](https://www.toyo-seikan.co.jp/product/beverage/can/dican/) は
    **「350mlの缶胴径は211径」までは公表しているが、mm を書いていない**
  - 大和製罐・アルテミラ製缶は製品ページに**寸法表が無い**
  - JIS Z 1571:2016 の表4・表8に飲料缶の内径と高さの規定はあるが、
    **無償ミラー（kikakurui.com）は数値の列が欠落している**（HTMLを取得して確認済み）
  - → **進めるには製缶メーカーへの問い合わせか、JIS規格票の購入が要る**
- 市販銘柄とボトル品名の対応（「い・ろ・は・す2L＝2L角ボトル(A)」等）
- **内寸の測り方。** 定義を明記しているのは**ダイワ**（「内寸は一番狭い所、外寸は一番広い所」）と
  **シマノ**（**内寸を「中央値」と「底部」の2つ公表**。底部は中央値の93〜95%）。
  **ロゴス・アイリスオーヤマは未記載。**
  ⚠️ **ダイワのサイズ表記の順は「縦（奥行）×幅×高さ」**（シークールキャリーIIのページに明記）。
  床の本数計算では最初の2つが入れ替わるだけなので**既存記事の計算結果は変わらない**
- **容量Lの算出方法。どのメーカーも公表していない。** ロゴスは「※容量表記は目安です」と明記している
- 保冷剤を「壁に立てかける」「上にボトルを立てる」が実際に安定するか（記事03は幾何のみ）
- **記事06のキーワード（「クーラーボックス 内寸60cm」）の SERP。** `serp-check.md` は
  **サイズ型を「勝てない」と判定している。** DuckDuckGo では EC が上位を占めつつ釣り系メディアも入っていた。
  **Google での確認は未実施**（`docs/article-06-uchinori-nagasa.md`）
- **カセットコンロの SERP。** サジェストで需要は確認したが、**順位の勝ち目は見ていない**
  （`docs/article-07-donabe-konro.md`）。`serp-check.md` にも行を足していない
- **五徳の径と土鍋の鍋底の径。** どちらも**全メーカー非公表。**
  「小さい鍋は鍋底16cm以上」を満たすかは公表値からは判定できない
- 候補ジャンル15件のうち、クーラーボックス以外の検索需要（`docs/genre-candidates.md` は全件未検証）
- 消費者庁のステマ規制 運用基準PDF（未読）

## 触ってはいけないところ / 踏みやすい罠

- ⚠️ **日本語の太字。** 閉じの `**` が約物（`」。`）の直後にあり後ろに文字が続くと効かない。
  **ビルドが検出して落とす**（`assertNoRawEmphasis`）。書き方は `site/README.md`
- ⚠️ **SVGの文字の重なり。** 日本語は字面が広く、フォントサイズ分の行間では足りない。
  **目視ではなく `getBBox()` で交差判定する**（検査スクリプトは `site/README.md`）
- ⚠️ **WebFetch（要約モデル）に数値を読ませない。** JIS のページで、
  **HTMLに存在しない数値（「内径66.7mm・高さ130mm」）を返してきた。**
  **数値は curl で取得して自分の目で grep する**
- ⚠️ **岩谷産業（イワタニ）は curl で 403。** ブラウザで開いたうえで、
  **ページ内から `fetch()` で同一オリジンの他機種ページをまとめて取ると速い**（記事07で使った手）。
  ただし**極・アモルフォはスペック表がJSで後読み**されるので、fetch では本体サイズが取れない
- ⚠️ **無印良品・ニトリはアクセス制限で取得できなかった**（curl がタイムアウト）
- ⚠️ **curl で 403 を返すメーカーがある**（シマノ・アイリスオーヤマ）。**ブラウザ（preview_start + javascript_tool）で開けば読める。**
  ダイワの製品ページは **JSレンダリングなので curl では中身が空**。同じくブラウザで開く
- ⚠️ **メーカーの公表値にも表記ミスがある。** アイリスオーヤマのブランドサイトは20Lの外寸を
  **「55.7×34.5×33.cm」と桁が欠けた状態で載せている。** ニュースリリース側の33.8cmを採った。
  **数字が変なときは、同じメーカーの別ページ（ニュースリリース等）を当たる**
- ⚠️ **`site/content/site.json` の `origin`。** canonical と sitemap がこの値を使う
- ⚠️ **`affiliateEnabled` を true にするとビルドが意図的に例外を投げる。**
  リンク解決の実装が先。フラグだけ立てても公開されない安全装置
- **原稿の正は `site/content/articles/`。** `drafts/` は使わない（2026-08-24 に廃止）
- `wrangler dev` を起動したまま新しい記事を足すと**アセットを拾わない**。dev を再起動する
- ワークツリーで作業を始めたら `cd site && npm install` が要る（`node_modules` は共有されない）

## 次にやること

1. **記事を書く（あと3本）。** 書けるものが残っている：
   - **土鍋メーカーをもう1社**（銀峯陶器だけでは「号数が社外でも不統一」を示せていない）
   - カセットコンロのプレート適合（イワタニは適合表PDFを公表している）
   - **コールマンの内寸が公表されているかを調べる。** サジェストに「コールマン クーラーボックス 何リットル」
     「クーラーボックス 20l コールマン」があるが、**当サイトはまだコールマンを1製品も扱っていない**
   - 保冷剤を2枚以上入れたとき
   - 何本持っていきたいか **→** 必要な内寸（**逆引きなら書ける。** 「何人分」は一次情報から出せない）
   - 350ml缶は何本（**缶の一次情報が取れたら**。上記のとおり足踏み中）
2. **Google Search Console を見る。** sitemap は自動更新される（現在13URL）。順位が付き始めたら
   `docs/serp-check.md` の収益モデルを実測値で更新する
3. 判断待ち1・2 の回答を反映する

```bash
cd site && npm run dev      # http://localhost:8787
cd site && npm run build    # dist/ に出力
```

## ドキュメントの地図

| ファイル | 中身 |
|---|---|
| `docs/affiliate-program-facts.md` | 料率・審査・規約・PA-API・ステマ規制。**一次情報で確認済み** |
| `docs/serp-check.md` | 13キーワードのSERP目視／需要確認／**収益モデル** |
| `docs/genre-candidates.md` | 候補ジャンル15件（**全件未検証**） |
| `docs/article-01-…` 〜 `article-07-…` | 各記事のリサーチ記録と未検証事項 |
| `site/README.md` | サイトの実装。デザインの実測値、設定の理由、罠 |
