# HANDOFF

最終更新: **2026-08-31**

**このファイルだけ読めば再開できる**状態を保つこと。古くなった記述は消して書き直す。履歴は git log にある。

---

## いま何をしているのか

Amazon の商品比較サイト「**寸法で選ぶ**」を運営している。
**カタログに書かれていない寸法**を、メーカー公表値から計算して答える記事を積む。

**2026-08-25 に利用者から「運営を任せる」指示を受け、記事執筆フェーズから運営フェーズに入った。**
手順の下敷きは `docs/operating-plan.md`（リベのノウハウ図書館記事の STEP1〜5 ＋ 公式との差分6点）。

| | |
|---|---|
| 公開URL | **https://sunpou.nexeed-lab.com/** |
| リポジトリ | `github.com/oshima0627/amazon-affiliate`（`main` が本番） |
| サイトのソース | `site/` |
| デプロイ | **`main` に push すると Cloudflare Workers Builds が自動でビルド＆デプロイ**（2〜3分） |
| Search Console | **登録済み。ドメインプロパティ `sc-domain:sunpou.nexeed-lab.com`** |
| 運営者 | 大島 直孝 / info@nexeed-lab.com |
| 記事数 | **19本**（審査のサイト要件「オリジナル記事10件以上」は10本目で満たした） |
| カテゴリ | クーラーボックス（9本）／**カセットコンロ（4本）**／脚立・踏み台（6本） |
| X | **@sunpou_lab**（2026-08-25 開設。投稿3本＋**予約9本＝2026-09-12まで埋まっている**。フッターにリンク出力あり） |

## ★ いちばん大事な状態：**2026-08-25 に測定が動き始めた**

`検証済み` — 本番で確認済み。

| | |
|---|---|
| アソシエイトID | **`sunpou-22`**（2026-08-25 申請完了。支払い・税務情報も入力済み） |
| 本番のリンク | **324本すべて生きている**（プレースホルダの残り 0） |
| リンクの形 | `https://www.amazon.co.jp/s?k=<商品名>&tag=sunpou-22` の**検索リンク**。`rel="nofollow sponsored noopener"` |
| 開示（Amazon規約側） | 全ページに出力。**運営規約第5条の文言と完全一致** |
| PR表記（景表法側） | 全記事に出力 |

⚠️ **本審査はこれから。** 申請から**180日以内に他人による3件の適格販売**が必要
（**期限は 2027年2月中旬ごろ**）。自己購入・家族への依頼はカウントされない。

### ★ 2026-08-25 に審査要件を原文で照合した（`docs/shinsa-check-2026-08-25.md`）

**記事の質は要件を満たしている。未達は「3件の適格販売」だけ。**

| 要件 | 現状 |
|---|---|
| 少なくとも10件の投稿 | **19本 ○** |
| 60日以内に更新／公開／運営主体が本人 | **○** |
| 独自コンテンツ（第三者資料には「重要な解説、分析、または変容」） | **○**（計算・突き合わせ・逆算がこれに当たる） |
| 不適切なサイト (a)〜(g) に該当しない | **○** |
| **180日以内に3回以上の適格販売** | **0件 ✗ ← ここだけ** |

⚠️ **「一度却下された申請を再度審査することはできません」**（ヘルプ原文）。**やり直しがきかない。**

⚠️ **記事をこれ以上増やしても合否は動かない。** 律速は**流入**。

**★ 検索リンクは規約上どうか（今回はじめて確認）**
> 特別リンクは…**商品とリンクしなければなりません**。商品リスト（**検索結果**…を含みます。）と
> 乙サイトのページをリンクした場合、乙は、特別リンクと関連させた乙のサイト上に
> **追加のオリジナルコンテンツも含めなければなりません。**

→ **禁止ではない。条件つきで認められている。当サイトは条件を満たしている。**
⚠️ **ただし原則は「商品とリンク」で検索結果は例外。324本すべて検索リンクは原則から外れている**（判断待ち4に直結）。

**★ X は審査対象外だと確認した**
アカウントの管理 > ウェブサイトとモバイルアプリの変更 に登録されているのは
**`https://sunpou.nexeed-lab.com` の1件のみ。X は登録されていない。**
審査は「提出したすべてのサイト」が対象で、**SNSを登録すると最低500フォロワーが要件になる。**
⚠️ **Xにアフィリエイトリンクを貼った瞬間に登録義務が生じ、500フォロワーの壁が審査要件に入る。現在0。絶対に貼らない。**

### ★★ 2026-08-31 の測定：**順位は問題ではなかった。律速は表示回数の絶対量**

詳細は `docs/measurement-2026-08-31.md`。⚠️ **GSCのデータは 8/28 まで（3日遅れ）。**

| | 8/29 | **8/31** |
|---|---|---|
| GSC 表示回数 | 7 | **22** |
| GSC クリック数 | 0 | **0** |
| GSC 平均掲載順位 | 17.4 | **15.1** |
| 表示のあったページ数 | 3 | **6** |

**★ 3本が1ページ目に入っている。** `/coolerbox/500ml-honsuu/` **7.4位**（11表示）／
`/coolerbox/horeizai-honsuu/` **7.5位**／`/coolerbox/uchinori-nagasa/` **9.0位**。

⚠️ **前回の「順位が2ページ目だからクリック0」という見立ては外れていた。** 順位は悪くない。

**★ クリック0は統計的にまだ何も意味しない。**
表示11回・7.4位で、CTRを10%と強気に仮定しても期待クリックは1.1回、**0回になる確率は33%**。
3%なら72%。→ **タイトルとディスクリプションを触るのは早い。触っても効果を測れない。**

**★★ 期限までに必要な量を逆算した。表示回数を 5〜16倍にしないと届かない。**
残り約168日で適格販売3件。`適格販売 = 表示 × サイトCTR × 0.20 × 0.05`（後ろ2つは `serp-check.md` の**未検証**の仮定）。

| サイトCTR仮定 | 必要な1日あたり表示 | 現状(3.7)の何倍 |
|---|---|---|
| 3% | 59.5 | **16倍** |
| 5% | 35.7 | **10倍** |
| 10% | 17.9 | **5倍** |

⚠️ **記事を増やすだけでは届かない。** 19本で3.7回/日。同じ質で倍にしても7.4回/日で桁が足りない。

**★ `serp-check.md` のルールが実データで裏づけられた。**
「カタログにない数値」の記事（500ml-honsuu・horeizai-honsuu）が**1ページ目**、
型番クエリ（フィクセル 17l/22l）を拾った `daiwa-shimano` が**34.3位＝4ページ目**。
→ **方針は当たっている。当たっている方向にだけ量が足りない。**

**推奨は「すでに1ページ目にある3本を上位に押し上げる」**（`docs/measurement-2026-08-31.md` 第5節）。
⚠️ **ただし、その3本がどのクエリで出ているかは匿名化で見えない**（22表示中20件が非表示）。
**表示がもう少し増えるまで狙うクエリは確定できない。**

**★ インデックスは実質完了した。**
8/29 リクエスト分の抜き取り2件（`/kyatatsu/omosa/`・`/cassette-konro/donabe-nangou/`）はどちらも**登録済み**。
**記事19 `/cassette-konro/erabikata/` は未登録だったので 8/31 にリクエストした。**
⚠️ **「ページのインデックス登録」レポートはまだ「データを処理しています」。**

### 2026-08-31 に記事側の不具合を3つ直した

| | 直した内容 |
|---|---|
| 図の重複 | **eyecatch と同じ画像が本文にもあって同じ図が2回出ていた記事が6本**（HANDOFFには3本と書いてあったが実際は6本）。本文側の説明的なaltを回収して先頭のeyecatchに移し、キャプションにも出す。**原稿は書き換えていない** |
| 引用の飲み込み | `/coolerbox/daiwa-shimano/` で「KEEP 46 はおよそ46時間…」の一文が、直前の引用に空行がなくて引用ボックスに入っていた（Markdownの遅延継続） |
| 計測ツールの取りこぼし | `tools/check-link-position.py` が目次を除外できていなかった（目次のclassを `toc toc--inline` に変えたのに正規表現が完全一致のまま）。**58.8% が 60.3% に水増しされていた** |

**上2つはビルドで落とすようにした**（`assertNoLazyBlockquote` / eyecatch重複チェック）。**バグを戻して落ちることを確認済み。**

**リンク位置：`/coolerbox/horeizai-honsuu/` を 58.8% → 17.3% にした**（記事が計算例に使っている S2000 の製品カードを結論の直後に置いた）。
⚠️ **残り1本 `/coolerbox/daiwa-shimano/`（40.2%）は直していない。判断待ち6に回した。**

### 2026-08-29 の測定（1つ前）：インデックスの詰まりが解けた。表示回数が 0 → 7

詳細は `docs/measurement-2026-08-29.md`（前回は `docs/measurement-2026-08-26.md`）。

| | 8/26 | **8/29** |
|---|---|---|
| **GSC 表示回数（3か月）** | **0** | **7** |
| **GSC 平均掲載順位** | — | **17.4**（＝2ページ目） |
| **インデックス登録済みの記事** | **0本**（トップのみ） | **11本 / 18本** |
| GSC クリック数 | 0 | **0** |
| アソシエイトのクリック | 3 | **4**（注文0・紹介料¥0） |
| Cloudflare Visits / PV（7日） | 20 / 40 | **24 / 48** |
| **Cloudflare の外部 Referer** | **0** | **0（変わらず）** |

**★ 8/26 にリクエストした10件は、10件すべてが「登録済み」になった。**
`/coolerbox/erabikata/` の前回クロールは **2026/08/26 10:46:28 ＝ リクエスト当日**。
→ **「インデックス登録をリクエスト」は効く。** これは実測で確かめた。

**★ `/kyatatsu/kaidan/` はリクエストしていないのに登録済みだった**（クロール 2026/08/28 4:07:14、参照元はサイトマップ）。
→ **Google は自力でも取りに来はじめている。ただし3日で1本だけ。リクエストのほうが速い。**

**★ 初めて出たクエリとページ**（データは 8/26 まで。最終更新は 8.5時間前）

| クエリ | 表示 | | ページ | 表示 |
|---|---|---|---|---|
| フィクセル 17l | 1 | | `/coolerbox/500ml-honsuu/` | 3 |
| フィクセル 22l | 1 | | `/coolerbox/daiwa-shimano/` | 3 |
| （残り5件は閾値で非表示） | | | `/coolerbox/horeizai-honsuu/` | 1 |

**3本ともクーラーボックスの集客記事。収益記事は0表示。クリックは全体で0。**
⚠️ クエリはシマノ「フィクセル」の型番系＝`serp-check.md` が「ECが独占」と判定した領域。**2ページ目に着地している。**

**★ 8/29 に10件リクエストした（本日の上限）。内訳は記事7本＋カテゴリページ3つ。**
記事は `/cassette-konro/` 3本と `/kyatatsu/` の tenban-takasa・fumidai-kyatatsu・omosa・secchi。
カテゴリは `/kyatatsu/` `/coolerbox/` `/cassette-konro/`（3つとも未リクエストのまま「検出 - インデックス未登録」だった）。
→ **これで記事18本すべてが「登録済み」か「リクエスト済み」になった。**

⚠️ **表示回数7は 8/26 までのデータ。** 8/26〜8/28 に入った10本の効果はまだ数字に出ていない。
**次に見るときに伸びていなければ、そこで初めて順位の問題として考える。**

⚠️ **GSC の「ページ」レポートは 8/29 時点でもまだ「データを処理しています」。** 一覧では見られない。**URL検査で1本ずつ。**

⚠️ **アソシエイトの4クリックを「外部の人が押した」と読んではいけない。**
Cloudflare の24訪問は**全部 direct** で、自分の確認アクセスと**切り分けられない**。
**クリック率の分子には使えない。** `serp-check.md` の仮定 20% / 5% は**据え置き**。

⚠️ **Xは3日たっても流入を1件も生んでいない。** 初投稿は 8/26 21:00、以降 8/28 まで投稿は流れている。
それでも Referer に `x.com` は0。**graduated access の影響と切り分けられない。**

⚠️ **国別は 日本16／米国7／アイルランド1。** 日本語のニッチ記事の読者としては不自然で、
**bot除外を抜けた自動アクセスの疑いがある**（未検証）。

**ここから測るのは2つだけ。**

```
① 記事 → Amazon のクリック率   … 分母は Cloudflare Web Analytics、分子はアソシエイト・セントラル
② クリック → 購入率            … アソシエイト・セントラルのレポート
```

**律速は「クロールされない」から「順位が2ページ目でクリックが0」に移った。** 診断の全体像は `docs/monetization-review.md`。

### ★ 2026-08-31 にサイトの構成を直した（利用者から「構成がださい」の指摘）

**根拠は NN/g の2本。`検証済み` — ローカルの dev サーバで実測して確認した。**
- 「ユーティリティリンクの置き場所はフッター」 https://www.nngroup.com/articles/footers/
- 「表は行の見出しを固定し、スクロールできることを見た目で示す」 https://www.nngroup.com/articles/mobile-tables/

| | 変更前 | **変更後** |
|---|---|---|
| ヘッダーのナビ | カテゴリ3＋固定ページ3の**6項目** | **カテゴリ3項目だけ**。固定ページはフッターへ |
| 375px でのナビの溢れ | `scrollWidth 706px` / 表示幅 338px → **368px が画面外**（スクロールバーも非表示） | **15px**（最後の項目のパディング分だけ） |
| フッターのナビ | ヘッダーと同じ6項目 | **6項目のまま**（ここが正しい置き場所） |
| 記事ページの目次 | **本文冒頭とサイドバーに2つ同時に出ていた** | **同時に見えるのは片方だけ**（901px以上=サイドバー版／900px以下=本文中版） |
| トップのサイドバー | 「このサイトについて」＝`site.description` で**本文リード文と一字一句同じ**。「カテゴリー」も重複 | **サイドバーごと出さない**。1カラム・本文860pxに畳む |
| トップの h1 | `寸法で選ぶ`（ヘッダーのロゴと同じ文字列が40px下に再掲） | **タグライン**「カタログに書いていない寸法で、モノを比べる」 |
| カテゴリへの導線 | **サイドバーだけ**。900px以下ではサイドバーが本文の下に落ちるので、スマホは記事19本を通り過ぎるまで出ない | **本文先頭のカテゴリカード3枚**。どの幅でも最初の画面に出る |
| 早見表の先頭列 | 横に送ると「どの行か」が消える | **`position: sticky` で固定**＋左右端にスクロールの影 |
| サイドバーの見出し | **紺ベタの帯が4本**積み上がり、本文より目立っていた | 小さい文字＋紺の細い下罫 |
| パンくず | 記事タイトルが入って**2行に折り返す** | 1行に切って末尾を省略記号 |
| サイトマップ | 記事本文用の h2（縦棒＋上に5.6remの余白）で間延び。日付がリンクに地続き（「…早見表）2026-08-29」） | 詰めた索引見出し＋日付を別行に |
| 記事カードのサムネ | 190px（1200x630のグラフが線の束にしか見えない） | 240px |
| `--muted` (#6f7680) | `--bg` の上で **4.47:1 = AA(4.5:1) に未達** | **#6a717b = 4.80:1**（`--surface` の上でも 4.56:1） |

**実測での確認（27ページ全部・ローカル dev）**
- 全27ページで ヘッダーナビ=3／フッターナビ=6／サイドバーはトップと404だけ無し
- 375px でトップ・サイトマップ・404・記事すべて **横スクロールなし**（`scrollWidth - innerWidth = 0`）
- 記事ページで見えている目次は **1つだけ**（もう片方は `getClientRects().length === 0`）
- 早見表を `scrollLeft=400`（最大759）にしても先頭セル「4人で9号土鍋を囲む」は左端 1px に残る

⚠️ **ダークモードは入れていない。** `color-scheme: light` 固定は参考サイトに合わせた既存の判断で、
今回の指摘の範囲外と見て触っていない。やるなら別作業。

### ★ 2026-08-26 に Amazonリンクの見た目を直した（A・B 完了。`docs/link-appearance-2026-08-26.md`）

**A: `.buy` にスタイルを付けた／B: 文言の二重表示をやめた。C・D は未着手。**

| | 変更前 | **変更後** |
|---|---|---|
| `.buy` のCSS | **1行も無し**（記事内リンクと同じ見た目） | **琥珀色のボタン**（`--warn-bg`/`--warn-line`＋文字`#7a4a00`）、下線なし、末尾に `↗` |
| コントラスト比 | — | **通常 7.11:1 ／ ホバー 10.24:1**（どちらもAAA） |
| リンク文言 | 全324本が「<商品名>をAmazonで見る」 | **139本を「Amazonで見る」に短縮**（185本はフル表記のまま） |
| リンクを含むセルの文字数 | 中央値36／最大**130** | 中央値**28**／最大**75** |
| 最大セル幅（実測・canvas） | 1658px | **1047px（−37%）** |

**⚠️ 直したバグ：本文中のリンクで日本語が壊れていた。**
`/cassette-konro/nenshou-jikan/` の「いちばん長いのは**イワタニ カセットフー スマート CB-SMT-1をAmazonで見る**の78分でした。」
→ 表示ラベルを商品名だけにして解消。**本番に出ていた。**

**★ モバイル375pxを実測した（これまで3回「未測定」と書いてきた項目）。**
**ページの横スクロールなし／表もカードもはみ出さない／タップ目標は WCAG 2.5.8（AA）を満たす。**
⚠️ **ただし収益記事2本だけ、表の中の最初のボタンが画面外**（脚立596px・クーラーボックス436px の位置）。
**16記事は画面内。** カードが表の外の押せる場所として効いているが、冒頭の「30秒で決めるなら」の表には
画面内に押せる場所が無い。**直し方は判断待ち**（`docs/link-appearance-2026-08-26.md` 第4-E節）。
⚠️ **計測の手口：`resize_window` では viewport が変わらない**（1920のまま）。
**幅375pxの iframe を作って `contentDocument` を測る。**

**★ C（製品カード）も入れた：7枚。** 早見表の外に押せる場所を作った。
中身は商品名＋1行の説明＋**自前の計算値**＋ボタン（画像も価格も無い）。
**カードの数字が記事の表と食い違うとビルドが落ちる**（`assertCardNumbers`。わざと壊して確認済み）。
記法は ```` ```card ```` フェンス（`site/README.md`）。コントラストは最低 5.6:1 で AA 以上。

⚠️ **`HANDOFF.md` に書いてあった「全記事で最初のリンクが本文の20%以内」は誤りだった。**
描画テキストで測ると `/cassette-konro/donabe-nangou/` だけ **33.5%**。カードを足して **20.4%**
まで下げたが、**まだ 0.4ポイント超えている。** 他の17本は 19.3% 以下。

**★ D（検索語）も一部やった：長谷川工業とピカの16語・延べ44本を書き換えた。**
17語の標本では **上位3位以内に着地するのは65%**。「一般名詞（脚立）を外す」仮説は
**長谷川工業とピカでは改善（RHB-12a 4位→1位、SEC-S120 5位→1位ほか）**したが、
⚠️ **アルインコでは悪化した**（CCA-80K 1位→3位。1位がフィットネスバイクになる）。
**一律のルールは使えない。ブランドごとに測るしかない。** 適用したのは個別に実測した語だけ。

**⚠️ 記法が増えた：`[[LINK:商品名::表示]]`。区切りは `|` ではなく `::`。**
`resolveLinks()` は **Markdown → HTML の後**に走るので、表の行に `|` を書くと
**セルの区切りとして先に解釈されて表が壊れる**（2026-08-26 に `|` で実装して実際に壊し、
検索URLが表のHTMLを飲み込んだ）。書き方は `site/README.md`。

**検証（すべて実測）**：全ページで表の列数がそろっている／`tag=sunpou-22` 欠け0／
`rel="nofollow sponsored noopener"` 欠け0／開示・PR表記とも維持／未解決の `[[LINK:` 0。

---

### 調査で分かっていること（規約）

**「いい感じに見せる」に規約待ちの要素は無い。C・D も今すぐできる。**

| 分かったこと | 状態 |
|---|---|
| **`.buy` のCSSが1行も存在しない** | `build.mjs` はクラスを付けているが `styles.css` に定義が無い。**324本が記事内リンクと同じ見た目**（`#0077c6`・太字・下線・`display:inline`）。本番DOMの計算済みスタイルで確認 |
| **商品画像は使えない** | 規約上 Creators API 経由のみ。その API は**「過去30日間に適格販売10件」**が条件で、実機で3条件すべて ❌ |
| **価格・在庫も使えない** | 同上（Amazon提供のリンクか Creators API のみ） |
| **SiteStripe も URLコピーだけ** | 実機確認。画像リンク・ウィジェットの選択肢が無い |
| 配置 | 324本中 **322本（99%）が表の中**。セルの表示文字数は中央値36・**最大130** |
| **文言の二重表示** | 同じ行に商品名が既出なのにリンク文言で繰り返しているものが **89本（27%）** |
| 手本 | **脚立の記事はすでに直っている**（繰り返さず計算値を添える形）。クーラーボックスの記事が古い書き方のまま |

⚠️ **審査合格の「180日で3件」より、API解禁の「30日で10件」のほうが厳しい。** 画像はかなり先。
⚠️ **検索リンクの着地を1件だけ実測した**（`ダイワ クールラインα3 S1000X` → 42件・先頭は別シリーズ）。
**324本すべてがこうかは未検証。**
⚠️ **見た目を変えた効果はまだ測れない。** 2026-08-29 に11本がインデックスに入ったが、GSC のクリックは0で、
Cloudflare の24訪問は全部 direct ＝ 自分のアクセスと切り分けられないため。

---

## 決まっていること（利用者の判断・2026-08-25）

| # | 決定 | 効き方 |
|---|---|---|
| 1 | **X等のSNSを併用する** | **アカウント作成はご本人**（エージェントは行わない）。**投稿はエージェントが代行してよい**（2026-08-25 に利用者の指示で `@sunpou_lab` から3本投稿した） |
| 1-b | **SNSにはアフィリエイトリンクを貼らない**（2026-08-25 決定） | **記事URLだけ貼って自サイトに送る。** 理由：①500フォロワーの壁を回避 ②未登録URL＝契約解除リスクを回避 ③記事に139本のリンクがあるので導線は完成済み。**自サイト経由でも3件はカウントされる**。詳細は `docs/sns-plan.md` |
| 2 | **もしもアフィリエイト経由にしない** | **Amazon公式アソシエイト単独。** 楽天併記もしない。`resolveLinks()` は Amazon のトラッキングIDだけを扱えばよい |
| 3 | **2つ目のカテゴリ「カセットコンロ」を作った** | `showCategoryNav` が自動で有効になり、ナビ復活・カテゴリページの noindex 解除 |
| 4 | **申請は記事10本を揃えてから** | 公式のサイト要件が「オリジナル記事10件以上」。180日タイマーを満たした状態で使い切る |
| 5 | **収益記事2本で冒頭の表のボタンが375pxだと画面外になる件は、直さない**（2026-08-26 決定） | **理由：①「30秒で決めるなら」に1枚カードを置くとどれを選ぶかが恣意的になり、「決めるのは読者、根拠を出すのが運営」という立て方が崩れる ②列順を変えると「入れたいものから引く」構造が壊れる ③未インデックスで効果を測れないうちに語り口を変えるリスクを取る意味が薄い。** 表の外の押せる場所はカードが担う（`docs/link-appearance-2026-08-26.md` 第4-E節） |

---

## 立ち上げの作業は全部終わった（2026-08-25）

| # | 作業 | 状態 |
|---|---|---|
| 1 | 記事を10本にする | **済** |
| 2 | 記事02のリンク位置0箇所を埋める | **済**（8箇所） |
| 3 | 収益記事を作り、集客記事から導線を張る | **済**（`/coolerbox/erabikata/`） |
| 4 | `resolveLinks()` を実装 | **済**（検索リンク方式。理由は `site/README.md`） |
| 5 | `affiliateEnabled: true` に切替、開示2種類を実機確認 | **済**（本番で確認） |
| 6 | リンクを記事前半に置く（クッキー24時間対策） | **済**（全記事で本文20%以内） |
| 7 | クリック率・購入率の測定 | **開始した。⚠️ ただし記事が未インデックスなので、まだ何も測れない** |

## 次にやること

**★ 律速がまた移った。「順位が2ページ目」ではなく「表示回数の絶対量が5〜16倍足りない」。**
根拠は `docs/measurement-2026-08-31.md`。インデックスは実質完了したので、そこはもう律速ではない。

0. **★ 9/3〜9/5 にもう一度 GSC を見る。** 見るのは**表示回数だけ**。
   8/31 時点で 3.7回/日。**これが伸びているか、頭打ちかで次が変わる。**
   ⚠️ **クリック数とCTRは見ても判断材料にならない**（標本が少なすぎる。第2節の確率計算のとおり）。
   ⚠️ **同時に、クエリ別レポートに新しいクエリが出てきていないかを見る。**
   いま22表示中20件が匿名化で見えず、**そのせいで「どのクエリを狙うか」が決められない。**
1. **★ 表示が増えて狙うクエリが見えたら、1ページ目の3本を上位に押し上げる。**
   対象は `/coolerbox/500ml-honsuu/`（7.4位）・`/coolerbox/horeizai-honsuu/`（7.5位）・`/coolerbox/uchinori-nagasa/`（9.0位）。
   ⚠️ **クエリが確定するまで着手しない。** 当て推量で本文を触ると、効果を測れないまま構成だけ壊れる。
   仮の狙いは `serp-check.md` の「クーラーボックス 500ml 何本」（**未検証**。1位は知恵袋）。
2. **記事19 `/cassette-konro/erabikata/` のインデックスを確認する。** 8/31 にリクエスト済み。
   ⚠️ **同じURLを再送信しない**（順番も優先順位も変わらない・ダイアログ原文）。
3. **X は流入0のまま。予約は 9/12 で切れる。**
   ⚠️ **効果0のまま伸ばすかは 9/12 が近づいたら判断する**（判断待ちに近い）。
4. **記事を増やすかどうかは、いま決め打ちしない。**
   ⚠️ **19本で3.7回/日なので、線形なら5倍に100本近く要る。量では届かない。**
   ⚠️ **脚立は記事11〜16で集めた寸法を使い切った。** 7本目を書くなら**新しい一次情報の取得から**。
   未使用で残っているのは「はしご時長さ」と「はしごの接点」（RHB・RYZB・PRH-FX・SCLが公表）。
   ただし**接点の高さが何を指すのかを長谷川のサイズ目安表で読み解くところから**必要
   （`https://www.hasegawa-kogyo.co.jp/support/size.php` の「はしご時の接点までの高さ」の節）
5. **D（検索語）の続き：126語のうち110語が未測定。** ⚠️ **測るなら利用者の判断が要る**（判断待ち5）。**急がない。**

## 公開済みの記事（19本）

| URL | 役割 | 内容 |
|---|---|---|
| `/coolerbox/erabikata/` | **収益記事** | **33製品の早見表。** 用途（500ml／2L／保冷剤／魚の長さ／自重）から内寸を逆に引く。**リンク51箇所** |
| `/coolerbox/500ml-honsuu/` | 集客 | 500mlが何本入るか。同じ20Lで15〜18本 |
| `/coolerbox/2l-tateru/` | 集客 | 2Lは立つか。深さ310mmが分かれ目。1.5Lは2Lとほぼ同じ高さ |
| `/coolerbox/horeizai-honsuu/` | 集客 | 保冷剤で何本減るか。同じ20Lに1枚で6本と18本 |
| `/coolerbox/horeizai-maisuu/` | 集客 | 保冷剤は何枚入るか。減らさずに入るのは**1〜7枚**で製品によって7倍違う |
| `/coolerbox/yoryo-uchinori/` | 集客 | 容量Lの正体。外寸の体積の28.9〜40.0%しか中に使えない |
| `/coolerbox/daiwa-shimano/` | 集客 | **KEEPとCOOLは同じJIS簡便法の「時間」で比較できる** |
| `/coolerbox/uchinori-nagasa/` | 集客 | 大型15製品を内寸の長辺順に。80Lと60Lは長辺が同じ85cm |
| `/coolerbox/coleman-uchinori/` | 集客 | **コールマンは内寸を「範囲」で公表。** 26Lクラスで深さ350mm、**7製品中6つで2Lが立つ** |
| `/cassette-konro/erabikata/` | **収益記事** | **イワタニ11機種の早見表。** 使いたい鍋の幅と「ボンベ1本で何分」から引く。**リンク26箇所** |
| `/cassette-konro/nenshou-jikan/` | 集客 | **ボンベ1本は強火連続で55〜78分。** 250g÷ガス消費量では9機種すべて短く出る（注記の測り方が違う）。**火力が強い機種は鍋も小さく燃焼時間も短い** |
| `/cassette-konro/bombe-honsuu/` | 集客 | **農水省「1人1週間6本」の中身を計算。** イワタニの用途別試算表を足すと、6本に届くのは10℃で全部まかなうときだけ（5.8本）。防災文脈で季節に依存しない |
| `/cassette-konro/donabe-nangou/` | 集客 | 同じ9号でも幅31〜32.5cm。「10号まで」の機種より10号土鍋が大きい |
| `/kyatatsu/erabikata/` | **収益記事** | **42製品の早見表。** 届きたい高さ − 身長 ＝ 必要な足場。踏台は天板高さ、脚立は使用最大高さでそろえた。**リンク60箇所** |
| `/kyatatsu/tenban-takasa/` | 集客 | **型番の「180」は天板高さとは限らない。** 専用脚立は型番＝天板高さ、はしご兼用は9〜12cm低い。**立てるのは型番より40cm下** |
| `/kyatatsu/kaidan/` | 集客 | **伸縮脚21〜44cmでは階段に届かない。** 前後の脚は3〜10段離れ、必要な高低差は69〜230cm。**「階段用」は前後の脚長が違う別カテゴリ** |
| `/kyatatsu/fumidai-kyatatsu/` | 集客 | **踏台と脚立の境目は天板高さ80cm。上わく付きなら1.61mでも踏台。** 同じ0.51mに両方あり、**上わくは天板の60cm上** |
| `/kyatatsu/omosa/` | 集客 | **足を置ける高さでそろえ直すと、0.5〜0.6mに立つのに1.7〜6.3kg（3.7倍）。** カタログの天板高さ順では比べられない |
| `/kyatatsu/secchi/` | 集客 | **広げると奥行がしまうときの1.7〜14.5倍。** 幅は変わらない。奥行は天板高さの0.6〜1.2倍で、踏台のほうが広がる |

**内部リンクの構造**：
- クーラーボックス：集客9本 → 収益記事 `/coolerbox/erabikata/`
- **脚立・踏み台：集客5本 → 収益記事 `/kyatatsu/erabikata/`**（2026-08-25 に構築。5本とも本文2〜4%の位置に引用ブロックで導線）
- **カセットコンロ：集客3本 → 収益記事 `/cassette-konro/erabikata/`**（2026-08-29 に構築。3本とも本文の最初の `## ` の直前に引用ブロックで導線）
⚠️ **「最初のリンクが本文の20%以内」は全記事では成り立っていない**（2026-08-29 に測り直した）。
**超えているのは2本：`/coolerbox/horeizai-honsuu` 58.8% ／ `/coolerbox/daiwa-shimano` 40.2%。**
`/coolerbox/uchinori-nagasa` が 19.9% でぎりぎり。**残り16本は15.7%以下。**
⚠️ **2026-08-26 に書いた「17本は20%以内、donabe-nangou だけ20.4%」は誤りだった。**
目次を本文に含めたまま測っていた可能性がある。**測り方は `tools/check-link-position.py` に固定した**
（目次・日付・PR表記・図版キャプションを除いて測る）。
`/cassette-konro/donabe-nangou/` は導線を入れて **20.4% → 15.7%** に下がった。

---

## 検証済みの事実（これを前提に動いてよい）

### Amazonアソシエイト

- 審査＝**オリジナル記事10件以上＋直近60日以内の更新＋申請から180日以内に3件の適格販売**（2026-08-25 に原文で再確認済み）
- ⚠️ **却下されたら再審査はない**（ヘルプ原文「一度却下された申請を再度審査することはできません」）
- **検索結果へのリンクは条件つきで可**。条件は「特別リンクと関連させた追加のオリジナルコンテンツ」
- **申請した時点でリンクの作成・掲載が可能**になる。**180日のタイマーは申請時点から動く**
- **自己購入も家族への依頼もカウントされない**
- **媒体をまたいで合算される**（ブログ2件＋X1件で可）
- **リンク経由なら紹介した商品以外が買われても報酬になる**
- **クッキーは24時間**（カート投入で89日）→ 結論とリンクを記事前半に置く
- **紹介料の1,000円上限は2024-08-07に撤廃済み**
- ⚠️ **Amazon Content Partners（+1%）は米国限定。日本からは登録できない**
- ⚠️ **価格を手打ちで書けない。** PA-APIは売上が立つまで使えない。
  さらに **PA-API v5 は 2026-05-15 でサービス終了済み。今後使うなら Creators API**（アソシエイトのお知らせで確認）
- ⚠️ **2026-04-20 施行の規約改定：Amazonにリンクする有料広告・ブースト広告を使うと不適格販売になる。** 広告で流入を作る手は使えない
- ⚠️ **開示は2種類必要**（Amazon規約の文言＋景表法ステマ規制のPR表記）。両方とも実装済み

### 収益の前提

- 紹介料率は**2〜8%**。**クーラーボックス240円／カセットコンロ120円（15ジャンル中の最下位）／スーツケース960円**
- 試算（クリック率20%・購入率5%と仮定）：**1セッション2.4円 → 月1万円に約4,167セッション**
- **ロングテール1記事1位＝250セッション/月**とすると、**月1万円に約17記事が1位**
- ⚠️ **判断軸は「単価 × 料率」の一本。** 料率表だけで絞ると間違える
- ⚠️ **報酬額とSERPの勝ち目は逆相関している**（`serp-check.md`）

### 勝てるキーワードは2種類だけ（`serp-check.md`）

1. **メーカーをまたぐ横断比較**（1社では書けない）
2. **カタログに無い実測値・累計コスト**

→ サイズ・型番系はECが独占、規格の解説はメーカーが独占していて勝てない。

### テーマを決めるときの手順

1. **一次情報が取れるかを先に確かめる**（350ml缶はこれで落ちた）
2. **Googleサジェストで需要を見る**（`/complete/search`, hl=ja, gl=jp を curl）。aramakijake の「0」はデータ欠損
3. **検索意図のうち寸法で答えられない部分は、記事の冒頭で明示して切る**
4. **単価（単価×料率）も見る** ← 記事07で見落とした

### 保冷力の表示

- **ダイワ「KEEP」とシマノ「COOL」は同じ物差し。** どちらも **JIS S 2048：2006 の簡便法**
  （外気40℃／本体容量の25%の角氷／8時間後の残存率）で**氷が溶け切るまでの時間**に換算した値。**比較してよい**
- ⚠️ **アイリスオーヤマの「保冷日数」は別物差し**（容量比40%の氷・30℃9h/20℃15h）。**換算できない**
- ⚠️ 両社とも「目安であり保証値ではない」と明記。**優劣は書かない**

### 内寸・外寸の測り方

- **ダイワ**：「内寸は一番狭い所、外寸は一番広い所」。**サイズ表記の順は「縦（奥行）×幅×高さ」**
- **シマノ**：内寸を「**中央値**」と「**底部**」の2つ公表（底部は中央値の93〜95%）。**底部を使う**
- **ロゴス・アイリスオーヤマは未記載**
- **シマノは計算の前提ボトルも公表**：PETボトル500ml **Φ66×207mm**／2L 90×105×305mm／板氷 130×240×65mm

### 脚立で分かったこと（記事11・3社の公表値で確認）

- **専用脚立は「型番の数字＝天板高さ」。** アルインコ BSA-A（7サイズ）、ピカ SEC-S（7サイズ）とも一致
- **はしご兼用脚立は型番より天板が9〜12cm低い。** 長谷川 RHB（5サイズ）、ピカ MCX（6サイズ）
  → **同じピカでも SEC-S210 は天板2.10m、MCX-210 は1.98m**
- **長谷川工業だけ「使用最大高さ」を公表している。** RHB は5サイズすべて **型番の数字ちょうど −40cm**
- **天板には乗れない。またがり・座りも禁止行為。** 長谷川FAQ。
  ⚠️ ただし同社は「**軽金属製品協会と製品安全協会の決定で、法律の規制ではない**」と明記している
- **踏台と脚立の境目は天板高さ80cm**（上わく付きなら80cm以上でも踏台と呼ぶことがある）
- **尺は30cm刻み。** ピカの製品名から **3尺＝09、6尺＝180**（正確な1尺は30.303cm）
- 長谷川のサイズ目安表：**作業高さ＝身長＋天板高さ、身長160cm基準**
  ⚠️ **これは天板に乗れる踏台の式。** 脚立に当てはめるなら使用最大高さで計算する（記事ではそう明記した）
- **収納時の奥行は3社とも16〜18cm。** 効くのは高さのほう
- **踏台と脚立の境目は天板高さ80cm**（長谷川FAQ）。**上わくが付けば80cm以上でも踏台**（アルインコTBF-8は1.61m）
- **上わくなしの踏台は2社とも 0.30 / 0.56 / 0.79m で打ち止め**（長谷川SE・アルインコCCA-K。高さが完全に一致）
- **上わくは天板から60〜62cm上**（全高−天板高さ。2社ともサイズによらず一定）
- **同じ0.51mに踏台（長谷川EFA-05）と脚立（ピカMCX-60）の両方がある。** 高さでは決まらない
- **広さでも決まらない。** 長谷川SWH（脚立）の天板350×300mmは踏台EFAの286×255mmより広いのに脚立
- **仕様表の項目が違う。** 脚立には「使用最大高さ」、上わく付き踏台には「全高」、踏台には天板高さだけ
- **収納の奥行は上わく付きのほうが薄い**（EFA-08 9.6cm ＜ SE-8a 18cm）。ただし全長は伸びる（150cm vs 85cm）
- **伸縮脚の調整量は 長谷川21cm(RZS)・31cm(RYZB)／ピカ31cm(SXJ)／アルインコ44cm(PRT-FX・PRH-FX)／ピカ階段用45cm(SCL)**
- **建築基準法施行令23条：住宅の階段は蹴上げ23cm以下・踏面15cm以上。** その他は22cm以下・21cm以上
- **伸縮脚では階段に届かない。** 前後の脚は3〜10段離れ、必要な高低差は69〜230cm
- **「階段用」は前脚と後脚の長さが違う別カテゴリ**（ピカ かるノビSCL・アルインコ PRH-FX）。
  **ピカSCLは仕様表に「階段使用時」と「平地使用時」の天板高さを別に載せている**
- ⚠️ **記事11で「アルインコとピカは使用最大高さを公表していない」と書いたのは誤りだった**（図版・本文とも修正済み）。
  **BSA-A と SEC-S に無いだけで、アルインコの PRT-FX / PRH-FX には「有効高さ［使用最大高さ］」の欄がある**

---

## 判断待ち（利用者に投げてある）

| # | 内容 |
|---|---|
| 1 | **Amazonbot を許可のまま維持するか戻すか。** +1%が取れない以上、いまは「対価なしでAmazonに学習を許している」状態 |
| 2 | **保冷力だけを扱う記事（ランキング等）を作るか。** KEEPとCOOLが同じ物差しと分かったので事実としては書ける。ただし**サイト名「寸法で選ぶ」の射程外** |
| 3 | **次のジャンルを単価で選び直すか。** 高単価のスーツケース(960円)は `serp-check.md` が「勝ち目なし」と判定済み |
| 4 | **一部を ASIN 直リンクに差し替えるか。** いまは全139本が検索リンク（型番違い・生産終了で別物を指す事故を避けるため）。**成約率は直リンクのほうが高いはず**だが、ASINが正しい変種を指しているかを1件ずつ確認する手間がかかる。**測定で検索リンクのクリック率が出てから判断するのが筋** |
| 5 | **検索語の残り110語を測るか。** A・B・C と D の一部は 2026-08-26 に完了。**測るには本人のアカウントからAmazonへ100回以上の自動リクエストが要る。審査中で却下されたら再審査がない**ので判断待ち（`docs/link-appearance-2026-08-26.md`） |
| 6 | **`/coolerbox/daiwa-shimano/` の最初のリンクが 40.2% と遅い件をどうするか。** 20%以内に入れる定石は「製品カードを前半に置く」だが、**この記事は「どちらが上とは書きません」と本文に明記している。** カードを置くと本文と矛盾する。放置するか、記事の立場のほうを変えるか |
| 7 | **審査期限（2027年2月中旬）に間に合わない可能性をどう扱うか。** 表示回数が5〜16倍要る（`docs/measurement-2026-08-31.md` 第3節）。**落ちたら再申請できない。** 間に合わない見込みが強まったとき、申請を維持するのか別の手を打つのかを決めておく必要がある |

---

## 未検証（`検証済み` に昇格させないこと）

- **350ml缶の寸法。書けない状態が続いている。** 東洋製罐は「350mlの缶胴径は211径」までしか公表せず mm を書いていない。大和製罐・アルテミラ製缶は寸法表なし。JIS Z 1571 の無償ミラーは数値の列が欠落
- **五徳の径と土鍋の鍋底の径。** どちらも全メーカー非公表
- **土鍋の号数が銀峯陶器以外でも不統一か**（無印良品・ニトリはアクセス制限で取得できず）
- **カセットコンロと「クーラーボックス 内寸60cm」のSERP。** サジェストで需要は見たが順位の勝ち目は未確認
- **X のハンドル候補5つ（`sunpou_lab` 等）が本当に空いているか。** `x.com/<handle>` が404を返すことは確認した
  （既存アカウントは200なので判定方法自体は機能している）が、**404は凍結・予約済みでも返りうる**
- **X で数字ネタが伸びるか／SNS経由で3件に届くか。** このアカウントでの実績はゼロ。
  ⚠️ **しかも X は新規アカウントを graduated access 扱いにしていて、「検索やトレンドに出にくい」と明記している**
  （投稿画面に出た「Unlock more on X」の案内）。**開設直後に伸びなくても、それだけでは何も分からない**
- もしもアフィリエイト経由のAmazon料率（**採用しないと決めたので、確認する必要もなくなった**）
- 市販銘柄とボトル品名の対応／保冷剤を壁に立てかけたときの安定性
- 候補ジャンル15件のうちクーラーボックス以外の検索需要（`docs/genre-candidates.md` は全件未検証）
- **はしご兼用脚立が型番より低い理由。** 構造から説明はつくが、**どちらのメーカーも書いていない**
- **階段用モデル（ピカSCL・アルインコPRH-FX）が何段まで対応するか。** 前後の脚長差そのものが非公表
- **アルインコ PRT-FX の「天板高さ − 有効高さ ＝ 59cm」の理由**（長谷川は30cm）
- **実際の住宅階段の踏面・蹴上げの分布。** 法令は上限下限を決めているだけ
- **アルインコが踏台の定義文をサイトに書いているか**（記事13で探したが見つからなかった）
- **天板寸法の横断比較。** 公表しているのは長谷川の EFA（286×255mm）と SWH（350×300mm）だけ
- **上わく付き踏台に何cmまで登れるか。** 踏台の仕様表に使用最大高さの欄がない

---

## 触ってはいけないところ / 踏みやすい罠

- ⚠️ **日本語の太字。** 閉じの `**` が約物（`」。）`）の直後にあり後ろに文字が続くと効かない。
  **ビルドが検出して落とす**（`assertNoRawEmphasis`）。書き方は `site/README.md`
- ⚠️ **SVGの文字の重なり。** **目視ではなく `getBBox()` で交差判定する**。
  さらに**図形の座標を実寸に逆算して本文の数値と一致するかも確認する**（今セッションで定着した手）
- ⚠️ **bash の出力に出た日本語を、そのままクエリや原稿に使わない。** 端末が cp932 なので
  `CS＋` が `CS白` に化ける。**2026-08-26 に化けた商品名でAmazonを引いて「商品名が怪しい」と
  誤った結論を出した。** 日本語を扱うときは**ファイルに書き出して Read ツールで読む。**
- ⚠️ **WebFetch・WebSearch の要約に数値も引用文も読ませない。** JISのページで**HTMLに存在しない数値を返してきた**。
  記事13では**「アルインコも踏台の定義を書いている」と返してきたが、自社サイトのどこにも見つからなかった**。
  **数値も引用文も curl か browser で取得して自分で grep する**
- ⚠️ **メーカーの公表値にも表記ミスがある**（アイリスは20Lの外寸を「33.」と桁欠けで掲載）。
  **変なときは同じメーカーの別ページ（ニュースリリース等）を当たる**
- ⚠️ **curl が 403 のメーカー**：シマノ・アイリスオーヤマ・**岩谷産業**。**ブラウザで開けば読める**
- ⚠️ **ダイワの製品ページは curl で取れる**が、**KEEPの定義はクールラインα3のページに無い**
  （シークールキャリーIIのページにある）
- ⚠️ **数値を記事に書いたら、必ずスクリプトで再計算して記事の値と突き合わせる。**
  記事17で「4.1本」と書いたが正解は4.0本（4.0488の丸め）。**検算スクリプトだけが見つけた**
- ⚠️ **イワタニは `navigate` で直接URLを指定するとトップにリダイレクトされる。**
  `iwatani.co.jp` のタブを開いてから**ページ内の同期 XMLHttpRequest** で他ページを取ると通る（記事17で実証）
- ⚠️ **イワタニはブラウザで開いてからページ内 `fetch()` で他機種をまとめ取りすると速い。**
  ただし**極・アモルフォはスペック表がJSで後読み**され、fetch では本体サイズが取れない
- ⚠️ **無印良品・ニトリは curl がタイムアウト**して取得できない
- ⚠️ **e-Gov 法令API v2 は本文を base64 の XML で返す。** そのまま grep しても日本語が1件もヒットしない。
  `base64.b64decode(json[...]['law_full_text']).decode('utf-8')` してから読む（`docs/article-12-kaidan.md` にコマンド）
- ⚠️ **「このメーカーは公表していない」と書くときは、シリーズの話か会社の話かを分ける。**
  記事11で「アルインコは使用最大高さを載せていない」と書いて誤りだった（別シリーズには欄がある）
- ⚠️ **イワタニの「極」「アモルフォ プレミアム」の仕様表は製品ページに無い。`/function/` という別ページにある。**
  製品ページのHTMLには「本体サイズ」の文字列すら存在しない。**「JSで後読み」は誤りだった**（2026-08-29 に訂正）
- ⚠️ **1つのページに複数商品の仕様表が載っていることがある。** イワタニのミニのページには
  併売の鍋「スクエアポット20」の表も並んでいて、**機械的に拾うと重量を取り違える**（1.0kg と 1.1kg）
- ⚠️ **「AはBに比例する」を2製品で確かめて一般化しない。** 記事18が9機種のうち2機種だけを見て
  「火力が強いほど燃焼時間が短い」と書き、**同じ表の中に反例があった**（2026-08-29 に訂正）。
  **順位相関を出すスクリプトを書けば1分で分かる**
- ⚠️ **`build.mjs` は front matter の `eyecatch` を必ず本文の先頭に出す。** 本文にも同じ画像を
  `![...]()` で書くと**2回出る**（既存3本がこの状態）
- ⚠️ **リンク位置を測るときは目次を本文から外す。** 含めたまま測ると数ポイント大きく出る。
  **`tools/check-link-position.py` を使う**
- ⚠️ **`site/public/` の画像を直しても、dev サーバは `site/dist/` の古いコピーを返す。**
  **`node site/build.mjs` を通してから getBBox で再検査すること**（同じ3pxの重なりを2回検出して気づいた）
- ⚠️ **Browser ペインが非表示だとスクリーンショットが撮れない**（5秒でタイムアウト）。
  **SVGの検査は `getBBox()` と座標の逆算で完結させる**
- ⚠️ **X の投稿画面は1回目の `type` が飲まれる。** 本文もALTも2回打つことになる。**必ず `zoom` で確認する**
- ⚠️ **フォーカスが乗っていない状態で `ctrl+a` を送ると、リテラルの「a」が入力されることがある**（ALT欄で実際に混入した）。
  **打ち込んだあとは必ず `zoom` で先頭から読む**
- ⚠️ **`find` は予約パネルが描画される前だと「開いていない」と返す。** スクリーンショットで開いていることを確かめてから `find` し直す
- ⚠️ **予約リストの件数を数えるとき、`document.body.innerText` では0件になる。**
  `document.querySelector('[role="dialog"]')` から `Will send on` を拾うと数えられる
- ⚠️ **X で ALT を保存するとモーダルの高さが変わる。** 古い座標でツールバーを押すと**モーダル外**を押してしまい、
  **書きかけが下書きにも残らず消える**（実際に1本作り直した）。**保存後に必ずスクショを撮る**
- ⚠️ **GSCのドメインプロパティは、サイトマップ欄に相対パスを入れると「アドレスが無効です」になる。フルURLで入れる**
- ⚠️ **GSCの「インデックス登録をリクエスト」は1日10件で打ち止め**（2026-08-26 に実測。11件目で「割り当て量を超えています」）
- ⚠️ **GSCの検索ボックスは、クリック直後に `type` すると入力が飲まれる。** 間に2秒待つ。
  **「リクエスト済み」トーストが出ている間はクリックがトーストに吸われる**ので先に「表示しない」を押す。
  **`inspect?...&url=` の直リンクは効かない**（サマリーにリダイレクトされる）
- ⚠️ **`site/content/site.json` の `origin`。** canonical と sitemap がこの値を使う
- ⚠️ **`affiliateEnabled` を true にするとビルドが意図的に例外を投げる。** リンク解決の実装が先
- ⚠️ **リベのノウハウ図書館記事のURLを公開記事に貼らない**（記事のレビュー欄で禁止されている）
- ⚠️ **煽り文句（数量限定・在庫僅少）は使わない。** 景表法の有利誤認リスク
- **原稿の正は `site/content/articles/`。** `drafts/` は使わない
- ワークツリーで作業を始めたら `cd site && npm install` が要る（`node_modules` は共有されない）
- heredoc で長い日本語Markdownを書くと**クォートで落ちることがある**。**Write ツールを使う**

```bash
cd site && npm run dev      # http://localhost:8787
cd site && npm run build    # dist/ に出力
```

---

## ドキュメントの地図

| ファイル | 中身 |
|---|---|
| `docs/operating-plan.md` | **運営手順。** リベ記事のSTEP1〜5／公式との差分6点／利用者とエージェントの作業分担／申請の実務 |
| `docs/sns-plan.md` | **SNSの選定と X アカウント作成キット。** ⚠️ **Amazon公式ヘルプ2ページの記述が食い違っている**（「全SNS可」vs「Instagram/Twitter/YouTube/Twitch のみ」）ことも記録 |
| `docs/monetization-review.md` | **収益設計の診断。** 弱点4つ（測定未開始・収益記事不在・AIO・単価）と打ち手 |
| `docs/shinsa-check-2026-08-25.md` | **審査要件の原文照合。** 7要件の現状／検索リンクの規約上の扱い／Xが審査対象外である確認 |
| `docs/affiliate-program-facts.md` | 料率・審査・規約・PA-API・ステマ規制。**一次情報で確認済み** |
| `docs/link-appearance-2026-08-26.md` | **Amazonリンクの見え方の調査。** `.buy` のCSSが無いこと／画像・価格が使えない規約上の根拠／打ち手4つ |
| `docs/measurement-2026-08-29.md` | **実測の記録（最新）。** ★ インデックスの詰まりが解け、表示回数が 0→7 になった回。GSCの罠4つ |
| `docs/measurement-2026-08-26.md` | 実測の記録。記事が1本もクロールされていないことをURL検査で確認した回 |
| `docs/measurement-2026-08-25.md` | 実測の記録（初回）。3つのダッシュボードの数字／サイトマップ再送信の顛末 |
| `docs/serp-check.md` | 13キーワードのSERP目視／需要確認／**収益モデル** |
| `docs/genre-candidates.md` | 候補ジャンル15件（**全件未検証**） |
| `docs/article-01-…` 〜 `article-07-…` | 記事01〜07のリサーチ記録と未検証事項 |
| `docs/article-08-10-notes.md` | **記事08〜10のデータ源と計算の前提**（収益記事・コールマン・保冷剤の枚数） |
| `docs/article-11-kyatatsu.md` | **記事11のデータ源。** 3社4シリーズの全寸法と、取得できなかったもの |
| `docs/article-19-konro-erabikata.md` | **記事19（カセットコンロの収益記事）。** ★ 極・アモルフォの仕様が `/function/` にあったこと／ミニだけ実寸公表／**記事18の誤りを訂正した経緯**／リンク位置の実測 |
| `docs/article-18-nenshou.md` | **記事18のデータ源と検算。** イワタニ9機種のガス消費量・燃焼時間／※1※2の条件の違い／ボンベ2種の内容量と缶重 |
| `docs/article-17-bombe.md` | **記事17のデータ源と検算。** 農水省×イワタニの突き合わせ／⚠️ 検算スクリプトが丸め誤りを見つけた件／岩谷を同期XHRで取る手口 |
| `docs/article-16-erabikata.md` | **収益記事の作り方。** 42製品の一覧の作り方／導線の張り方／使えなかったデータ |
| `docs/article-14-15-notes.md` | **記事14・15の前提。** 「足を置ける高さ」でそろえる根拠／設置÷収納の倍率／奥行÷天板高さの比率 |
| `docs/article-13-fumidai.md` | **記事13のデータ源。** 踏台7シリーズの全寸法／⚠️ **WebSearchの要約が「アルインコも定義を書いている」と返したが自社サイトで確認できず採用しなかった経緯** |
| `docs/article-12-kaidan.md` | **記事12のデータ源。** 伸縮脚6シリーズの全寸法／建築基準法施行令23条／e-Gov APIの取得手順／**メーカーの表記ミス2件** |
| `docs/x-drafts.txt` | **Xの投稿下書き。** 投稿済み／未投稿と、添える画像の対応 |
| `site/README.md` | サイトの実装。デザインの実測値、設定の理由、罠 |
