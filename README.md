# amazon-affiliate

Amazon の商品比較記事を作るためのリサーチ・原稿置き場。

## このリポジトリの目的

「毎年・ライフイベントごとに同じ悩みで再検索されるジャンル」を選び、
サイズ・規格・数値で決着がつく比較記事を積み上げる。報酬額の高さでは選ばない。

## ジャンル選定の条件

1. 用途が決まっていて、選ぶ基準が数値やサイズで比べられる
2. 型番が変わりにくく、1年後も同じ悩みで検索される
3. 検索して調べる人がいるのに、まともに比べた記事が少ない
4. 3,000〜15,000円（迷って調べる程度の金額）
5. 効果・効能を語る必要がない（薬機法・景表法の領域に入らない）

条件5により、加湿器・電動歯ブラシ・枕・シャワーヘッド・電気毛布などは意図的に除外している。
数値比較自体は可能だが、「〜に効く」を避けると記事が痩せ、避けないと表現規制の領域に入るため。

## ディレクトリ

| パス | 内容 |
|---|---|
| `docs/genre-candidates.md` | 候補ジャンル15件。検索KW・悩みの発生場面・比較基準・外す読者・次の悩み |
| `docs/affiliate-program-facts.md` | Amazonアソシエイトの料率・審査条件・規約制約・PA-API・ステマ規制。一次情報で確認 |
| `docs/serp-check.md` | 13キーワードのSERP目視／サジェストによる需要確認／収益モデルの試算 |
| `docs/article-01-coolerbox-500ml.md` | 記事01のリサーチ。一次情報のデータ・計算の検証・構成案 |
| `drafts/01-coolerbox-500ml.md` | 記事01の**初稿**（未公開）。価格に触れない構成 |
| `site/` | 公開用サイト。**Cloudflare Workers Static Assets**。手順は `site/README.md` |

## 記録のルール

- **検証済みの事実と、未検証の仮説を必ず区別して書く。** 未検証のものには `未検証` と明記する。
- 検索ボリューム・競合記事の薄さは、キーワードツールと実際の SERP 目視で裏取りしてから
  「検証済み」に昇格させる。推測のまま断定しない。
- 商品スペック（寸法・重量・規格）は必ず一次情報（メーカー公式・取扱説明書）で確認する。
  Amazon の商品ページの記載だけを根拠にしない。

## 現在のステータス

- 2026-08-24: 候補ジャンル15件を洗い出し（**検索需要・競合状況はすべて未検証**）。
- 2026-08-24: **15件すべての料率カテゴリを Amazon の商品ページで実地確認**（`docs/affiliate-program-facts.md` 第1節）。
  1件あたりの報酬は **120〜960円**。判断軸は料率単体ではなく **単価 × 料率**。
  - **機内持ち込みスーツケース（8% × 12,000円 ＝ 960円）が単独で突出**。2位に2.7倍差
  - 下位は カセットコンロ120円／チェーンロック140円／フロアポンプ160円／双眼鏡160円 で、上位の1/6〜1/8
  - 推定が2件外れていた：**折りたたみ傘は8%（上方修正）／カセットコンロは家電扱いで2%の可能性（下方修正）**
- 2026-08-24: **13キーワードの SERP を目視**（`docs/serp-check.md`）。⚠️ **前提が2つ崩れた。**
  1. **「サイズ・規格・数値で比べられる」は選定条件として機能しない。**
     サイズ・容量・型番のKWは楽天/Amazon/価格.comが独占し、規格・等級の解説はメーカー・専門店・
     公的機関が独占していた。**個人が勝てるのは「メーカー横断の比較」と「カタログに無い実測値・累計コスト」だけ**
  2. **報酬額と勝ち目が逆相関。** 報酬上位のスーツケース(960円)・折りたたみ傘(320円)は
     SERPで個人0件＝勝ち目なし。個人が入れているのは報酬240円の中位帯
  - → 着手候補は **①おむつ用ゴミ箱 ②ラベルライター ③クーラーボックス**（いずれも240円）
- 2026-08-24: 需要側を確認（`docs/serp-check.md` 後半）。
  - ⚠️ **無料ツール（aramakijake）はロングテールを「0」と返す。データ欠損であって需要ゼロではない。**
    Google サジェストで反証済み。**「0」を検証済みの事実として扱わないこと**
  - サジェストを見ると、3ジャンルすべて「勝てる型」のKWに実需があり、
    需要の中身（メーカー横断・カタログに無い実測値）が SERP の判定と一致した
  - **収益モデル：勝てるロングテールは1記事あたり月500〜2,000円。**
    月1万円なら**5〜20記事が1位を取れている状態**が必要（`未検証`の仮定3つを含む試算）
  - **最初の1記事の推しは「クーラーボックス 500ml 何本」**。知恵袋が1位＝まともな記事が無く、
    サジェストが本数バリエーションで埋まっている＝需要が構造的、かつ実測すれば独自データになる
  - ⚠️ 結論を最も左右するのは**リンククリック率と購入率**で、これは自分の記事で実測しかない。
    **最初の数記事の目的は「収益」ではなく「この2つを測ること」**
- 2026-08-24: **記事01に着手**（`docs/article-01-coolerbox-500ml.md`）。テーマは
  「クーラーボックスに500mlペットボトルは何本入るか」。
  ランニングコスト系（おむつゴミ箱・ラベルライター）を後回しにしたのは、**差別化点が価格で、
  立ち上げ期は価格を書けない**ため。寸法だけで完結するテーマを先に置いた。
  - ⚠️ **着手前の想定「実測しないと書けない」は外れた。** DAIWA と LOGOS は
    **500mlの本数を公式に公表**しており、非公表メーカーは**公表内寸から計算できる**
  - **同じ20Lで DAIWA 18本 / LOGOS 16本。容量Lでは本数が決まらない**（記事の核）
  - **計算式（内寸の床面積÷ボトル直径）が DAIWA の公表値8本を完全に再現。** 検証可能な形になった
  - 副産物：**カタログの「○本」は保冷剤込みの実用値**で幾何学的上限ではない（LOGOS が明記）
  - ⚠️ ~~最優先の未検証：500mlボトルの寸法~~ → **解決。ボトル製造メーカー2社が寸法を公表していた**
- 2026-08-24: 記事01の**初稿を作成**（`drafts/01-coolerbox-500ml.md`）。
  - **500mlペットボトルに決まった寸法は無い**（胴径59〜70.5mm・全高184.5〜230mm）。
    当初置いていた「直径6.5cm」は実レンジの中央値にすぎなかった
  - 計算式（⌊幅÷胴径⌋×⌊奥行÷胴径⌋）は **DAIWA の公表値3件と厳密一致、残り2件も計算レンジの内側**。
    → 単一値ではなく**レンジで出すのが正しい**
  - 副産物3つ：**カタログの本数は保冷剤込みの実用値**（LOGOS が明記）／
    **深さ22cmだと全高230mmの細長い銘柄が立たない**／
    **床面積が同じ858cm²でも本数が違い、ボトルの型で優劣が逆転する**
  - ⚠️ 残る未検証：市販銘柄と型の対応／内寸の測り方の統一性／格子以外の詰め方
- 2026-08-24: **公開用サイトを構築**（`site/`）。Cloudflare Workers Static Assets、Worker コードなし。
  Markdown を HTML に変換する自作ビルド（依存は marked のみ）。
  - **ローカル `wrangler dev` で検証済み**：canonical・OG・JSON-LD 出力／表5つが全て箱内で横スクロール／
    375px でページ自体は横スクロールしない／存在しないURLは **HTTP 404**／sitemap・robots 配信／コンソールエラーなし
  - ⚠️ **`affiliateEnabled=false` のうちはリンクも開示文言も出力しない。**
    リンクが無いのに「適格販売により収入を得ています」と書くのは事実に反するため
- 2026-08-24: **デザインを `tsuzukiblog.org` 参考に作り替え**（`site/public/styles.css`・`site/build.mjs`）。
  参考サイトの設計値を実測して合わせた：背景 `#fcfcfc`／本文 `#434347`／本文17px・行間1.6／
  H2は左に8pxの紺の縦棒＋極薄背景で見出し前の余白を大きく取る／リンクは太字＋下線／
  本文716px＋サイドバー304pxの2カラム。**目次を h2/h3 から自動生成**するようにした。
  - 検証済み：デスクトップ1280pxで2カラム＋サイドバーsticky／**375pxで1カラムに落ちサイドバーは下**／
    横スクロールなし／表5つは箱内でスクロール／本文リンクは太字下線・目次リンクはプレーン
  - 記事の出典に**メーカー公式ページへのリンク**を追加（Amazonリンクではないので規約と無関係）
- 2026-08-24: **参考サイトと同じ構成に拡張**（`site/README.md` に機能一覧）。
  グローバルナビ／パンくず／アイキャッチ／目次（本文＋サイドバーの2箇所）／図版キャプション／
  SNSシェア／関連記事／カテゴリーページ／記事カード／フッターナビ／ページトップ／
  運営者情報・プライバシーポリシー・サイトマップの各ページ。全ページ HTTP 200 を確認
  - **画像を自作**（`site/public/img/`）。写真素材は使わず SVG で作図。
    アイキャッチは記事の核（内寸22×39cmに5列3行＝15本）、本文図版は
    **床面積が同じ858cm²でも15本と12本に分かれる**ことを示す図
  - ⚠️ **著者プロフィールは入れていない。** 運営者の情報を持っておらず、捏造しないため。
    `about.md` に記入欄だけ用意した。**Amazonアソシエイトの審査前に埋めること**
  - ⚠️ og:image が SVG のため、SNSのカード画像は表示されない可能性がある（要PNG化）
- 2026-08-24: **GitHub と Cloudflare を Git 連携。** `main` に push すると自動でビルド＆デプロイされる。
  リポジトリ `oshima0627/amazon-affiliate` ／ Root directory **`/site`** ／ ビルド `npm run build` ／
  デプロイ `npx wrangler deploy`。非本番ブランチは `versions upload`（本番に出ない）
- 2026-08-24: **本番公開。** https://sunpou.nexeed-lab.com/ （Worker `sunpou` ＋ カスタムドメイン）
  - 本番で curl 検証済み：トップ200／記事は末尾スラッシュへ301→200／存在しないURLは404／
    canonical と og:url が実URLと一致
  - ⚠️ **robots.txt に Cloudflare の Managed content が差し込まれており、`Amazonbot` が Disallow になっている。**
    Amazonアフィリエイトのサイトで Amazon のクローラを拒否している状態。zone 設定由来なので
    リポジトリ側では直せない。**要判断**（Googlebot は通っているので検索流入は妨げていない）
  - **アクセス解析：Cloudflare Web Analytics を導入済み。** ビーコンが実際に
    `cloudflareinsights.com/cdn-cgi/rum` へ送信されるところまで確認。
    測っているのは**分母（セッション数）**で、クリック数と注文数はアソシエイト・セントラル側から取る
- 2026-08-24: アソシエイトの料率・審査・規約を一次情報で確認（`docs/affiliate-program-facts.md`）。
  記事構成に効く確定事項が3つ出た：
  1. **価格は手打ちで書けない**（リンク／API 経由のみ・日時と免責の併記が必須）。
     PA-API は売上が立つまで使えないので、**立ち上げ期の記事は価格に触れない構成にする**
  2. **クッキーは24時間**（カート投入なら89日）。結論を記事後半に置くと取りこぼす
  3. **審査はオリジナル記事10本＋180日以内に3件の適格販売**。アカウント取得は記事10本を書いた後
- リベシティのノウハウ図書館に **Amazonアソシエイト専用の記事が1本ある**ことを確認
  （2026-04-03 更新。前提は「スマホ1台・X／Instagram」で、SEO比較記事とは媒体が違う）。
  内容は概ね公式と一致しているが**6点の食い違い・不足**があり、そのまま信用はできない。
  詳細と訂正は `docs/affiliate-program-facts.md` 第0節。
  - 使える指摘は1つ：**申請とリンク掲載は記事が揃う前でも可能**で、3件の適格販売は**媒体をまたいで合算される**
  - リベのもう一つの型（ASP案件型ブログ）は1件2,000円以上の案件が前提で、
    Amazon の料率2〜8%（1件60〜600円）には転用できない。**収益モデルの再設計が未着手の課題。**
