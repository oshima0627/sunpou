# CLAUDE.md

## セッションの引き継ぎ（毎セッション必須）

### 始めるとき: `HANDOFF.md` を読む

リポジトリ直下の `HANDOFF.md` に前回までの状況が書いてある。**作業を始める前に必ず読む。**

### 終わるとき: `HANDOFF.md` を書き直す

**このファイルだけ読めば再開できる**状態に上書きする。

- **古くなった記述は消して書き直す。継ぎ足して長くしない。** 履歴は git log に残る
- **未検証の推測を「検証済み」として書かない。** 引き継ぎに残った誤りは、次のセッションが前提として積み上げてしまう
- 判断待ちで止まっているものは「判断待ち」として明記する

`README.md` はプロジェクトの定義（目的・条件・ディレクトリ）だけを持つ。
**時系列のログを README に積まない。** 現在の状況は `HANDOFF.md`、調査の中身は `docs/` に置く。

### 終わるとき: コミットして push する

**確認は取らずに実行する。** `main` に push すると Cloudflare が自動でビルド＆デプロイする。

```bash
git add -A && git commit -m "<何を変えたか>" && git push
```

作業ブランチにいる場合は `main` へマージするところまで行う。PR は不要（本人のみが使うリポジトリ）。
途中で終わるとき・失敗したときも、**そこまでの状態をコミットして残す。** 未完了は `HANDOFF.md` に書く。

## 事実の扱い

このサイトは「メーカー公表の一次情報から計算する」ことを売りにしている。記録も同じ基準で書く。

- **`検証済み` と `未検証` を必ず区別する。** 未検証のものには `未検証` と明記する
- **一次情報だけを根拠にする。** 通販サイトの商品説明・まとめ記事は根拠にしない
- 数値を書いたら**出典URL**を添える
- **実測していないものを「実測」と書かない。** 計算値は計算値と書く

## 記事を書くときの必須事項

詳細は `site/README.md`。特に踏みやすいもの：

1. **価格・在庫に触れない**（Amazon規約。PA-APIが使えるまで）
2. **日本語の太字は閉じの `**` が約物の直後に来ないようにする**（ビルドが検出して落とす）
3. **SVGの文字は `getBBox()` で重なりを検査する**（目視では見落とす）
4. 原稿の正は **`site/content/articles/`**。別の場所に下書きを置かない
