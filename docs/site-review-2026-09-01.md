# サイトレビュー（2026-09-01）

本番 `https://sunpou.nexeed-lab.com/` と `site/dist/`（`node site/build.mjs` を通した出力）を見た記録。
**すべて実際に取得・計測した結果。** 推測はその旨を書いた。

計測に使ったもの：本番へのcurl、Browserペイン（375×812 のモバイルエミュレーション）、
`dist/` を走査したスクリプト（`<article>` 内の `class="buy"` と `<table>` の対応、内部リンクの被リンク数）。

---

## A. 事実の誤り（このサイトの売りに直接効く）

### A-1. `/coolerbox/erabikata/` のアイキャッチ図が古い　`検証済み`

**収益記事の1枚目の図が、本文と違う数字を出している。**

| | 図（`coolerbox-erabikata-eyecatch.png`） | 本文 |
|---|---|---|
| 見出しの数字 | **26製品**（「内寸を並べた 26製品」） | **33製品**（title・H1・本文に10箇所） |
| 2Lが立つ数 | **306mm以上は 26製品中3つ** | **33製品中9つ。うち6つがコールマン** |

- 図の定義：`tools/figures/figures.py:651` `key="26"` ／ `figures.py:655` `"306mm以上は 26製品中3つ"`
- 本文：`site/content/articles/coolerbox-erabikata.md`（`33製品` が10箇所、`26製品` は**0箇所**）
- PNG を開いて目視でも確認した
- **OG画像 `site/public/img/og/erabikata.png` は「33製品」で正しい。** 記事を26→33製品に広げたとき、
  OG画像だけ焼き直してアイキャッチを直し忘れたと見られる（**この経緯自体は未検証**）

**なぜ検査をすり抜けたか（ここが本題）**

`tools/figures/build.py` の `check_numbers` は、図の数値を **旧SVG** と突き合わせている（`build.py:60-81`）。
つまり「PowerPoint 移行のときに数字を打ち間違えていないか」の検査であって、
**「図が本文と一致しているか」は一度も検査していない。**

さらに数値トークンだけの照合では捕まらない。「26」は本文に **「26Lクラス」** として存在するので、
`26製品` が消えていても素通りする。

⚠️ `CLAUDE.md` の「ビルドが3つ検査して落とす（数値が元図にあるか／…）」は正しいが、
**「元図」＝旧SVG であって本文ではない。** ここを取り違えると「図は検査済み」と誤解する。

### A-2. key数値が本文に見つからない図が他に5件　`要確認`

同じ方法で全31図を当たった結果、キー数値が本文に文字列として存在しないもの：

| 図 | 記事 | key |
|---|---|---|
| `coolerbox-2l-eyecatch` | `coolerbox-2l-tateru.md` | 51mm |
| `coolerbox-yoryo-danmen` | `coolerbox-yoryo-uchinori.md` | 6.3cm |
| `konro-donabe-haba` | `konro-donabe-nangou.md` | 1.3cm |
| `kyatatsu-kaidan` | `kyatatsu-kaidan.md` | 38cm |
| `kyatatsu-omosa` | `kyatatsu-omosa.md` | 3.0倍 |

**誤りだと確認したわけではない。** 単位の書き方が違うだけ（51mm ↔ 5.1cm、3.0倍 ↔ 3倍）の可能性が高い。
A-1 と違い、図と本文が正面から食い違っているのを確認できたのは `26製品` だけ。

---

## B. 収益導線：**押す場所が、モバイルではほぼ存在しない**　`検証済み`

未達の要件は「180日以内に3件の適格販売」だけ。そこに直接効くのがここ。

### B-1. アフィリエイトリンクの95%が表の中にある

`dist/` の `<article>` 内を数えた結果：

- **リンク総数 381本のうち、362本（95.0%）が `<table>` の中**
- **20記事のうち12記事は、表の外にリンクが1本も無い**
  （`2l-tateru` `500ml-honsuu` `coleman-uchinori` `daiwa-shimano` `horeizai-maisuu`
  `uchinori-nagasa` `yoryo-uchinori` `fumidai-kyatatsu` `kaidan` `kyatatsu/omosa`
  `secchi` `tenban-takasa`）
- 製品カード `.pcard` があるのは**6記事だけ**。**14記事はゼロ**

### B-2. その表が、モバイルでは26%しか見えない

375×812 で `/coolerbox/erabikata/` を開いて実測した（`scrollWidth` と表示幅）：

| 表 | 表の幅 | 枠の幅 | 見えている割合 |
|---|---|---|---|
| 「30秒で決めるなら、ここだけ」 | **1303px** | 337px | **26%** |
| 33製品の早見表（10列） | 1152px | 337px | 29% |

そして「30秒で決めるなら、ここだけ」の表では、**Amazonリンクは3列目（右端）にある。**
モバイルの初期表示では1列目（「入れたいもの」）しか見えず、
リンクは画面の外か、右端で切れた「nで見る ↗」の断片として出る（スクリーンショットで確認）。

`.table-wrap` には横スクロールの影（グラデーション）が付いていて配慮はある。
ただし**26%しか見えない表**に対しては、影だけでは足りない。

→ **モバイル読者にとって「決めたあとに押す場所」が実質ない記事が多い。**
`.pcard`（表の外に置く製品カード）は既に実装があり6記事で使われている。**残り14記事で使っていないだけ。**

### B-3. リンクの無い製品行がある　`検証済み`

`/coolerbox/500ml-honsuu/` の結論の表で、`キャプテンスタッグ CS＋コールドクーラーボックス20` の行だけ
リンクが無い（他の2行にはある）。**意図的かは未確認**（`CS＋` は端末で化ける商品名として
HANDOFF に記録がある。関係あるかは分からない）。

---

## C. 流入：律速が表示回数なら、ここが空いている　`検証済み`

### C-1. カテゴリ3ページの本文が0文字

`/coolerbox/` `/cassette-konro/` `/kyatatsu/` の中身は
**定型1文（「〜について、メーカー公式の寸法から計算して比べた記事です。」）＋記事一覧だけ。**
`<main>` から一覧を除いた固有の本文は**0文字**。

「クーラーボックス 内寸」のような head 語の受け皿になり得る位置なのに、順位を取る材料が無い。
被リンク（内部）は10本入っているので、クロールはされている。

### C-2. BreadcrumbList の構造化データが無い

パンくずのUIは出ている（`aria-label="パンくずリスト"`）が、**JSON-LD には `BreadcrumbList` が1件も無い。**
全27ページを確認した。出しているのは `Article` / `WebPage` / `Organization` / `WebSite` / `CollectionPage`。

`Article` に `author` も無い（`publisher` はある）。

### C-3. 関連記事が「同カテゴリの新着5本」なので、古い記事に何も入らない

`dist/` の被リンクを数えた結果（関連記事ブロック内のみ）：

| ページ | 関連記事からの被リンク |
|---|---|
| `/coolerbox/omosa/`（最新） | **13** |
| `/coolerbox/erabikata/` | 13 |
| `/coolerbox/2l-tateru/` | **0** |
| `/coolerbox/500ml-honsuu/` | **0** |
| `/coolerbox/daiwa-shimano/` | **0** |
| `/coolerbox/horeizai-honsuu/` | **0** |

**古い4本はどの関連記事にも出てこない。** 特に `/coolerbox/500ml-honsuu/` は
（`serp-check.md` で需要が最大とされているキーワードなのに）**本文からの被リンク2・関連記事0**。

日付順で機械的に出しているため、記事が増えるほど古い記事が内部リンクから落ちていく。

### C-4. タイトルが長い

記事タイトルは**39〜56字**（`<title>` の長さ）。モバイルのSERPは全角30字前後で切れるので、
差別化している後半（「同じ180でも天板の高さは10cm違いました」）が落ちる。
**表示回数ではなくCTRの話なので、律速そのものではない。**

---

## D. 速度・配信　`検証済み`

### D-1. トップページの画像が20枚・1.35MB

1200×630 の PNG を**約150px幅のサムネイル**として20枚出している。合計 1,353KB。
`srcset` は0件。WebP/AVIF も無い。全部 `loading="lazy"`。

そのうえ、150px に縮めた早見表の図は**文字がまったく読めない**（実機で確認）。
容量を払って、読めない絵を出している状態。

### D-2. CSS も画像も `max-age=0`

```
Cache-Control: public, max-age=0, must-revalidate
```

HTML だけでなく `/styles.css` と `/img/*.png` にも付いている（Workers Assets の既定）。
再訪のたびに全アセットを再検証する。`site/public/_headers` は存在しない。

なお HTML は brotli で配信されている（`Content-Encoding: br`）。404 も 404 を返している。

---

## E. 体裁

- **`.pagetop` が常時表示で本文に重なる。** `position: fixed` の44px角が右下に出っぱなし
  （`<a href="#">` だけでスクロール量に応じた表示制御が無い）。
  モバイルで表や本文の上に被るのを確認した
- 記事一覧の説明文が meta description そのまま（76〜128字）。カテゴリページで10本並ぶと読み流せない
- トップの「新着記事」は20本の日付順一列。**収益記事3本（選び方）が埋もれている**

## F. 運用

- **`HANDOFF.md` が923行・79KB。** `CLAUDE.md` の「継ぎ足して長くしない」に反している。
  「このファイルだけ読めば再開できる」状態からは離れつつある
- `site/.assetsignore` は効いていない（ファイル自身のコメントに記載済み。実害は無い）

---

## 直した記録（2026-09-01・同日）

**A〜E はすべて直した。F だけ残っている。** 数字は直したあとに測り直したもの。

### A. 事実の誤り

| | 直した内容 |
|---|---|
| A-1 | `figures.py:651,655` を 26 → **33**、`26製品中3つ` → **`33製品中9つ`** にして焼き直した。PNG を開いて目視で確認済み |
| A-1' | **同じ食い違いが本文にもあった。** 7記事が早見表を「26製品」と紹介したままだった（`500ml-honsuu` `coleman-uchinori` `daiwa-shimano` `horeizai-honsuu` `horeizai-maisuu` `uchinori-nagasa` `yoryo-uchinori`）。さらに `2l-tateru` は「26製品のうち2Lが立つのは3つだけ」と書いていた。**早見表の行を数え直して33製品・9つを確認**し、8記事すべてを直した |
| A-2 | 5件の「本文に無い数値」を1件ずつ当たった。単位の書き方の違い（図 cm ／ 本文 mm）は検査側で吸収し、**本当に本文に無かった4件は本文に書き足した**（`51mm はみ出す` `38cm 足りません` `外寸45.0×30.8cm` `28QT（約26L）`）。図の側を直したのは2件（`0.5〜0.6mの5製品` → 伸縮脚を除く旨に変更、`0.8m に足を置ける3つ` → 本文に3つの選び方を追記） |

**検査そのものを作り直した**（`tools/figures/build.py`）。

- 照合の相手を **旧SVG → 記事の本文** に変えた。移行の打ち間違いではなく、**図と記事のズレ**を捕まえる検査にした
- 数値だけでなく **「数値＋単位」** で照合する。単位の換算（5.6cm＝56mm）と末尾の0（6.3cm＝6.30cm）は吸収する。
  ⚠️ **末尾の0を落とす向きは認めない。** 図の `3.0倍` を本文の `3倍` で通したら、
  本文の無関係な「収納時の奥行は3倍以上」に当たって合格してしまった（下記 A-3 を見逃した）
- `deck.py` に `lines`（テキストボックス単位）を足した。`drawn` は run 単位なので「33」と「製品」が別々に入り、`33製品` を照合できなかった

### A-3. レビューには書いていなかった誤りが、直す途中で2つ見つかった

どちらも**ブラウザで実際に開いて気づいた**もの。

1. **`/kyatatsu/omosa/` の本文の図が、まったく違う説明のaltを持っていた。**
   画像は「0.8m前後に立つのに 2.5kg と 7.6kg（3.0倍）」の棒グラフなのに、
   alt は「0.5〜0.6mの製品で1.7kg〜6.3kg、3.7倍」と書いてあった（＝アイキャッチの説明）。
   しかも図は 0.5〜0.6m を扱う結論セクションに置かれていた。**altを画像の中身に直し、図を 0.8m のセクションへ移した**
2. **同じ記事で、図と本文の倍率が食い違っていた。** 図は3.0倍、本文の見出しは「0.8m前後でも2.2倍あります」。
   本文の表には 0.90m の SWH-12（7.6kg）が入っているのに、倍率は 5.6kg で計算していた。
   **どちらも残したうえで「そこまで含めると3.0倍」と明記した**（表の行は消していない）

**alt と図の照合も検査に足した**（`check_alt`）。これで**他に3件**見つかって直した
（`konro-donabe-haba` `kyatatsu-fumidai` `kyatatsu-secchi`。いずれも alt が記事の範囲の話を書いていて、図が見せている1製品の値と合っていなかった）。

### B. 収益導線

| | 前 | 後 |
|---|---|---|
| 表の外にリンクが1本も無い記事 | **12本** | **0本** |
| 製品カード `.pcard` | 6記事・16枚 | **20記事・30枚** |
| リンク総数（表の中の割合） | 381本（95.0%） | 398本（91.0%） |
| 最初のリンクが本文の20%超（**契約A3**） | **2本**（58.8% / 40.2%） | **0本**（最大21.0%→17.0%） |

- 14記事に製品カードを1枚ずつ入れた。**入れた数字はすべて、その記事のその製品の表の行から取っている**
  （`assertCardNumbers` が全カードで検査して通っている）
- `/coolerbox/daiwa-shimano` はカードの位置を結論の末尾から結論の直後へ上げて 21.0% → **17.0%**
- モバイルの表は**先頭列の固定と横スクロールの影がすでにあった**ので、足したのは
  **はみ出している表にだけ出す「→ 横にスクロールできます」**（幅を実測して付ける。CSSでは書けない）

### C. 流入

- **カテゴリ3ページに本文を書いた**（`site/content/categories/*.md`）。0文字 → 各600〜700字。
  `<title>` と `description` もカテゴリごとに書いた（`site.json`）
- **BreadcrumbList を全ページに出した。** 28ページ中26ページ（トップと404を除く全部）。
  全ページの JSON-LD を `JSON.parse` して**壊れ0件**を確認。404の空 `<script>` も直した
- **`Article` に `author` を足した**
- **関連記事を日付順から巡回に変えた。** カテゴリの収益記事を先頭に固定し、残りは
  「この記事の次」から順に取る。**被リンク0だった4記事が4〜5に**なり、0のページは無くなった

### D. 速度・配信

- **トップページの画像 1,353KB → 346KB（−74%）。** 幅480pxのサムネを別に焼くようにした
  （`tools/figures/build.py` が本体と一緒に出す）。CSSの枠は240pxなので2倍表示でちょうど等倍
- **`site/public/_headers` を置いた。** `/img/*` と `/styles.css` に `max-age=86400`。
  ⚠️ ファイル名にハッシュが無く、**図版は直すことがある**ので1日で頭打ちにしている。HTMLは既定のまま

### E. 体裁

- **ページ先頭へ戻るボタンを、600pxスクロールするまで出さないようにした。**
  ⚠️ `innerHeight` を基準にしたら、スクリプトが走る時点で 0 のことがあり先頭で出てしまった
  （プレビューのブラウザで実際に起きた）。**画面の高さに依存しない固定値にした**

### F. 残っているもの

- **`HANDOFF.md` の分量。** 手を付けていない
- `site/.assetsignore` が効いていない件（実害なし）
- **トップページの記事一覧は日付順のまま。** 収益記事3本が埋もれている点は直していない
- 記事タイトルが39〜56字で、モバイルSERPでは後半が切れる点も直していない

---

## この作業で確かめたこと・確かめていないこと

`検証済み`

- ビルド（`node site/build.mjs`）・図版ビルド（`python tools/figures/build.py`、検査3種＋alt照合）・
  `tools/check-link-position.py` がすべて通る
- 直したあとの数字（上の表）は、`dist/` を走査して測り直したもの
- ブラウザ（375×812 と デスクトップ）で実際に開き、図・カード・スクロール案内・ボタンの挙動を目で確認した

`未検証`

- **`_headers` が本番で効くか。** Workers Assets に置いただけで、**レスポンスヘッダはまだ確認していない**。
  デプロイ後に `curl -I https://sunpou.nexeed-lab.com/styles.css` で見ること
- **これらが表示回数・クリック・適格販売を動かすか。** 何ひとつ確認できていない。
  効くかどうかは 9/3〜9/5 の `/measure` 以降でしか分からない

