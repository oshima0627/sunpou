# SNS の選定（2026-08-25）

利用者から「**どのSNSが相性がいいか調べてほしい。そのSNSでアフィリエイト用のアカウントを作る**」という依頼。

---

## 1. ⚠️ まず、Amazon公式ヘルプの記述が食い違っている

`検証済み` — 2026-08-25 に両ページの本文を取得して確認した。**同じ公式ヘルプ内で内容が違う。**

| ページ | 記述 |
|---|---|
| [審査条件ヘルプ（G8TW5AE9XL2VX9VM）](https://affiliate.amazon.co.jp/help/node/topic/G8TW5AE9XL2VX9VM) | 「プログラム運営規約およびプログラムポリシーを遵守するものである限り、**すべてのソーシャルプラットフォームをご利用いただけます。**ただし**Amazonインフルエンサー・プログラム**に新規登録するためには、Facebook、Instagram、YouTube、TikTokをご利用ください」 |
| [複数URL登録のFAQ（GM3CX5D6DSLGVSJD）](https://affiliate.amazon.co.jp/help/node/topic/GM3CX5D6DSLGVSJD) | 「・**現在参加が認められているSNSはInstagram、Twitter、YouTube、Twitch.tv**となり、Facebookについては公開グループページ、ファンページは対象となりますが、個人ページでの登録は出来ません」 |

**前者は「全部OK」、後者は「4つだけ」。** 後者のほうが古い記述に見えるが、**どちらが現行か断定できない。**

→ **安全側に倒すなら、両方の記述で確実にOKなのは Instagram / X(Twitter) / YouTube / Twitch.tv。**
**Pinterest と TikTok は、後者の列挙に入っていない。**

## 2. SNS を登録するときの必須条件（公式）

`検証済み` — 上記2ページの本文より。

- **アソシエイトリンクを使うURLは、漏れなく全て登録する。**
  「登録が無いURLでの使用が確認された場合…**契約解除の対象**となる場合がございます」
- **公開状態でなければならない**（非公開SNSでの参加は不可）
- **フォロワー数／登録者数が見えるページであること**
- **オーガニックフォロワーが一定数必要。ほとんどの場合、最低ラインは 500**
- URLは個人ページまで正確に（`https://twitter.com` だけでは不可）
- **トラッキングIDはサイトごとに分けられる** → SNS経由とサイト経由を分けて測れる

---

## 3. ★ 設計上の結論：**SNSにアフィリエイトリンクを貼らない**

**SNSには自サイトの記事へのリンクだけを貼り、商品リンクは記事の中で踏んでもらう。**

理由は3つ。

| # | 理由 |
|---|---|
| 1 | **500フォロワーの壁を回避できる。** アフィリエイトリンクを貼らなければ、そのSNSは「アソシエイトリンクを使うURL」ではないので登録義務も条件も掛からない |
| 2 | **未登録URL問題（契約解除リスク）を根本から避けられる** |
| 3 | **記事にはすでに139本のリンクがある。** 記事に来てもらえれば導線は完成している。しかも**滞在中に読者が納得してから**リンクを踏むので、SNSから直リンクするより筋がいい |

⚠️ 当初「媒体をまたいで3件を合算する」ことを狙っていたが、**自サイト経由でも3件はカウントされる**ので、この設計でも目的は達成できる。

⚠️ **将来フォロワーが500を超えて、SNSに直リンクを貼りたくなった時点で登録すればよい。**
そのときは**必ず先に登録してから貼る。**

---

## 4. どのSNSが相性がいいか

### このサイトのコンテンツの性質

```
・数字と表と図（SVGの図版が10本ぶんある）
・「へえ」と思わせる発見型（80Lと60Lは内寸の長辺が同じ85cm、など）
・検索意図型（2Lは立つ？ 保冷剤は何枚入る？）
・季節性がある（クーラーボックス＝夏／カセットコンロ＝冬）
```

### 評価

| SNS | リンク | 数字・図との相性 | 日本での規模 | 制作コスト | 判定 |
|---|---|---|---|---|---|
| **X（旧Twitter）** | **本文に貼れる** | **高**（画像＋短文＋リンクが素直に出せる） | **大** | **低** | **◎ 本命** |
| **Pinterest** | 貼れる | **最高**（図解がそのまま資産になる・検索型で長寿命） | 中（`未検証`） | 低（**既存SVGを画像化して流用できる**） | **○ 併用候補** |
| Instagram | **フィード投稿に貼れない**（プロフィール／ストーリーズのみ） | 高 | 大 | 中 | △ 導線が弱い |
| YouTube | 貼れる | 中 | 大 | **高**（動画制作） | ✗ コストが見合わない |
| TikTok | 制約あり | 中 | 大 | **高** | ✗ 同上＋公式ヘルプの列挙に無い |
| Threads / Bluesky | 貼れる | 中 | 小 | 低 | △ 規模が小さい |

### 本命は X

- **リンクが本文に貼れる**のが決定的。Instagram はここで脱落する
- **数字ネタと相性がいい。** 「80Lと60Lは、まっすぐ入る長さが同じ85cmでした」は、そのまま1投稿になる
- **図版がすでに10本ある。** SVGをPNGにすれば画像付き投稿がすぐ作れる
- コストが低く、**記事を書くついでに出せる**

### 併用するなら Pinterest

- **図解の資産性が高い。** Xの投稿は数時間で流れるが、Pinterestのピンは**検索経由で何ヶ月も拾われる**
- **季節先取りに強い。** いまは8月末で**クーラーボックスの需要は終わりかけ、カセットコンロはこれから**。
  冬向けの土鍋ネタを今から積むのに向く
- ⚠️ **ただし Amazon 公式ヘルプの片方に Pinterest が列挙されていない。**
  → **第3節の設計（アフィリエイトリンクを貼らず記事に送る）なら、この問題は発生しない**

`未検証` — 日本国内の Pinterest 利用者数と、このジャンルでの実際の流入。**やってみないと分からない。**

---

## 5. 推奨する進め方

1. **まず X を1つ作る。** アカウント名はサイトと揃える（例：`@sunpou_lab`）
2. **プロフィールに記事URLを置く**（`https://sunpou.nexeed-lab.com/`）
3. **投稿は「数字＋図版＋記事リンク」の形。** アフィリエイトリンクは貼らない
4. **反応が出たら Pinterest を足す。** 既存のSVGを画像化して流用する
5. **フォロワーが500を超えて直リンクを貼りたくなったら、先にアソシエイトにURLを登録する**

### 測定

**SNS用に別のトラッキングIDを作る必要はない**（SNSに直リンクを貼らないため）。
分けて見るのは Cloudflare Web Analytics の**参照元**で足りる。

---

## 6. X アカウント作成キット（2026-08-25 用意）

⚠️ **アカウント作成はエージェントが行わない。** アカウント作成・パスワード入力は本人の操作。
**以下は「貼り付けるだけ」の状態にしたもの。**

### ★ 開設済み：**@sunpou_lab**（2026-08-25）

| | |
|---|---|
| ハンドル | **`@sunpou_lab`** → https://x.com/sunpou_lab |
| 表示名 | 寸法で選ぶ｜カタログにない数字 |
| bio | 下記D案（例示型）を改行で整形して設定 |
| アイコン／ヘッダー | `assets/sns/` の2枚を設定済み |
| Web欄 | `sunpou.nexeed-lab.com` |

⚠️ **X の Web 欄は `https://` と末尾スラッシュ付きだと「有効なURLを入れてください」で弾かれた。**
サイト側は正常（200・リダイレクト0・Twitterbotでも200・robots.txtも許可）だったので**X側の入力チェックの問題。**
**スキームと末尾スラッシュを外した `sunpou.nexeed-lab.com` で通った。**

**サイト側からの導線も張った**（2026-08-25）：
- **全ページのフッター**に `X @sunpou_lab`（`site.json` の `xHandle` で管理。空にすると出力しない。`rel="me"` 付き）
- **運営者情報ページ**の連絡先にも記載

### ハンドル候補（2026-08-25 に空き状況を確認｜記録として残す）

`検証済み` — `https://x.com/<handle>` を叩いて確認。**既存アカウント（X／elonmusk／amazonjp）は 200、下記5つは全て 404。**
⚠️ **404 は凍結・予約済みでも返りうるので、空きの保証ではない。** 実際に取れるかは登録画面で分かる。

| 候補 | 状態 | 備考 |
|---|---|---|
| **`sunpou_lab`** | 404（空きの可能性） | **第一候補。** ドメイン `nexeed-lab.com` と揃う |
| `sunpoulab` | 404 | アンダースコア無し |
| `sunpou_size` | 404 | |
| `sunpou_erabu` | 404 | |
| `nexeed_sunpou` | 404 | |

### ⚠️ 方針：**特定ジャンルを前面に出さない**（2026-08-25 見直し）

初版はクーラーボックスの数字（「同じ20Lでも15〜18本」等）をbioとヘッダーに入れていたが、
**利用者の指摘を受けて全部外した。**

**このアカウントは「寸法で確かめる」という方法の看板であって、クーラーボックス専門アカウントではない。**
いま記事がクーラーボックスに偏っているのは**たまたま先に書いたから**で、ジャンルは増やしていく。
プロフィールにジャンルを書き込むと、**後で増やしたときに自分で自分を狭めることになる。**

→ **プロフィールとヘッダーには「方法」だけを書く。ジャンルは投稿の側で出す。**

### プロフィール（そのまま貼れる）

**表示名**

```
寸法で選ぶ｜カタログにない数字
```

**自己紹介（bio・160文字以内 / 下記は91文字）**

```
カタログに書いていない寸法を計算しています。容量Lや号数では「何が入るか」は決まりません。すべてメーカー公表値からの計算で、実測ではありません。数字には必ず出典URLを付けています。
```

⚠️ **「クーラーボックス」「500ml」「2L」といった固有のジャンル名を入れない。**
代わりに「容量Lや号数」という**カタログ表記一般**の言い方にしてある（号数＝土鍋、QT＝コールマンにも掛かる）。

**ウェブサイト**

```
https://sunpou.nexeed-lab.com/
```

### 固定ポスト（プロフィールにピン留めする用）

**ジャンルを2つ出して「クーラーボックス専門ではない」ことを最初に見せる。**

```
カタログの数字は、知りたいことを教えてくれないことがあります。

「20L」のクーラーボックスに何が入るかは容量では決まらないし、
「9号まで」のカセットコンロに9号の土鍋が乗るとも限らない。

メーカーが公表している寸法から計算して、そこを確かめています。
実測ではなく計算値で、数字には必ず出典URLを付けます。

https://sunpou.nexeed-lab.com/
```

### アイコンとヘッダー（作成済み）

`assets/sns/` に置いてある。**サイトのファビコン（`site/public/favicon.svg`）と同じ寸法線モチーフ・同じ配色。**

| ファイル | 用途 | サイズ |
|---|---|---|
| `assets/sns/x-icon-400.png` | プロフィール画像 | 400×400 |
| `assets/sns/x-header-1500x500.png` | ヘッダー | 1500×500 |
| `assets/sns/xheader.py` | ヘッダーの生成スクリプト（文言を変えたいとき） | — |

⚠️ **ヘッダーは左下がアバターで隠れる**ので、その領域（おおよそ x 0〜300 / y 300〜500）に文字を置いていない。
生成スクリプトは**文字どうしの重なり・枠外・アバターとの干渉を実測で検査**してから保存している
（SVG図版で `getBBox()` を使っているのと同じ考え方）。

⚠️ 生年月日は X の設定で**公開範囲を選べる。** 個人情報なので、サイトの運営に必要ない限り公開にしない。

### 投稿の下書き（記事から切り出したもの）

⚠️ **どの投稿にもアフィリエイトリンクを貼らない**（第3節の設計）。**貼るのは記事URLだけ。**
⚠️ **煽り文句（数量限定・在庫僅少）は使わない**（景表法の有利誤認リスク／`affiliate-program-facts.md`）。

**A. 方法の投稿（ジャンルに依らない。アカウントの性格を示す）**

| # | 本文 | 図版 | リンク先 |
|---|---|---|---|
| A1 | カタログの数字が、知りたいことを教えてくれない例を集めています。クーラーボックスの「容量L」、土鍋の「号数」、コールマンの「QT」。**どれも“何が入るか”は別の数字で決まっていました。** | なし | `/` |
| A2 | このサイトのルールは3つです。①**一次情報だけを使う**（通販の商品説明は根拠にしない）②**計算式を記事に書く**（読者が検算できる形にする）③**数字には出典URLを付ける**。実測ではなく計算値だとも必ず明記します。 | なし | `/about/` |
| A3 | メーカーが公表している数字どうしが**食い違っていること**もあります。イワタニは「10号土鍋まで」と書いているのに、**その機種の本体幅（33.4cm）より10号土鍋（34〜36cm）のほうが大きい**。どちらも公表値です。 | `konro-donabe-haba.svg` | `/cassette-konro/donabe-nangou/` |

**B. カセットコンロ（いまの季節に出す）**

| # | 本文 | 図版 | リンク先 |
|---|---|---|---|
| B1 | 「9号土鍋まで」と書かれたカセットコンロ。ところが**同じ銀峯陶器の9号でも幅は31〜32.5cm**あります。号数は寸法を決めていません。 | `konro-donabe-eyecatch.svg` | `/cassette-konro/donabe-nangou/` |
| B2 | イワタニのカセットこんろ10機種を並べたら、**火力と乗る鍋の大きさは無関係**でした。最大4.1kWの機種が9号まで、**最小2.9kWの機種だけが10号まで**。 | なし | `/cassette-konro/donabe-nangou/` |

**C. クーラーボックス（需要期＝初夏に出す）**

| # | 本文 | 図版 | リンク先 |
|---|---|---|---|
| C1 | ダイワのクーラーボックス、80Lと60Lで「まっすぐ入る長さ」が**同じ85cm**でした。20L増えても伸びるのは深さだけ。 | `coolerbox-nagasa-eyecatch.svg` | `/coolerbox/uchinori-nagasa/` |
| C2 | 20Lのクーラーボックスに保冷剤を1枚。**床に寝かせると18本→6本、壁に立てかけると18本のまま。** 同じ箱・同じ1枚でこの差でした。 | `coolerbox-horeizai-oki.svg` | `/coolerbox/horeizai-honsuu/` |
| C3 | ダイワの「KEEP」とシマノの「COOL」、**どちらもJIS S 2048の簡便法で「氷が溶け切るまでの時間」に換算した同じ物差し**でした。名前が違うだけです。 | `coolerbox-daiwa-shimano-eyecatch.svg` | `/coolerbox/daiwa-shimano/` |
| C4 | アイリスオーヤマの15Lクーラーボックス、**外寸で測ると46Lの箱**でした。中身に使えるのは13.4L。 | `coolerbox-yoryo-eyecatch.svg` | `/coolerbox/yoryo-uchinori/` |
| C5 | 2Lペットボトルが立つクーラーボックスを探すなら**コールマン**でした。26Lクラスで内寸の深さ350mm。日本の釣り系メーカーは同クラスで220〜260mmです。 | `coleman-fukasa-eyecatch.svg` | `/coolerbox/coleman-uchinori/` |

⚠️ **図版は `site/public/img/*.svg` にある。X はSVGを受け付けないので、PNGに変換してから添付する。**

### 投稿の順番

`未検証` — 季節性は仮説。**2026年8月末時点でクーラーボックスの需要は終わりかけ、カセットコンロはこれから。**

```
最初        … 固定ポスト → A1 → A2   （アカウントの性格を先に見せる）
9〜11月     … B1・B2・A3            （カセットコンロ＝冬に向けて積む）
通年        … C3（保冷力の物差し）は季節を選ばない
来年の初夏   … C1・C2・C4・C5         （クーラーボックスの需要期）
```

**A（方法）を先に出すのが要点。** 最初に見えるのがクーラーボックスばかりだと、
**専門アカウントだと思われて後でジャンルを増やしにくくなる。**

### ⚠️ 記事の側も偏っている

いま**クーラーボックス9本／カセットコンロ1本**。
プロフィールを汎用にしても、**サイトを見に行った人には偏りがそのまま見える。**

→ **次に書く記事は、クーラーボックス以外にする。**
候補は `docs/genre-candidates.md` の未検証5件（踏み台・脚立／チェーンロック／タイヤチェーン／フロアポンプ）と、
カセットコンロの2本目（イワタニが[プレート適合表PDF](https://www.iwatani.co.jp/jpn/consumer/products/cg/useful/plate/)を公表している）。

---

## 7. 未検証

- **Amazon公式ヘルプ2ページの食い違いが、どちらが現行か。** 断定していない
- **日本国内での Pinterest の規模**と、このジャンルでの流入
- **X で数字ネタが実際に伸びるか。** このアカウントでの実績はゼロ
- **SNS経由で3件の適格販売に届くか**
