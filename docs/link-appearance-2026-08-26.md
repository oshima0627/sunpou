# Amazonリンクの見え方の調査（2026-08-26）

`検証済み` — コード・原稿・本番DOM・アソシエイト・セントラル・規約原文を実際に見て確認した。

## 結論

1. **`.buy` のCSSが1行も存在しない。** ビルドはクラスを付けているのにスタイル定義が無く、
   **Amazonへのリンクが記事内リンクと完全に同じ見た目**（`#0077c6`・太字・下線）で出ている。
2. **商品画像も価格も使えない。** 規約上 Creators API 経由でしか取得できず、
   その API は**「過去30日間に適格販売10件」**が条件。現在0件。
3. **SiteStripe も URL のコピーだけ**になっていて、画像リンク・ウィジェットの選択肢が無い。

→ **「いい感じ」は自前のデザインで作るしかない。** 逆に言えば、規約待ちの要素はひとつも無い。

---

## 1. いまリンクがどう出ているか（実測）

### コード

`site/build.mjs` の `resolveLinks()` が生成するのはこれだけ:

```html
<a class="buy" href="https://www.amazon.co.jp/s?k=<商品名>&tag=sunpou-22"
   rel="nofollow sponsored noopener" target="_blank"><商品名>をAmazonで見る</a>
```

**`site/public/styles.css` に `.buy` の指定は無い**（`grep -n "\.buy" public/styles.css` が0件）。
本番DOMの計算済みスタイルも、ふつうのリンクと同一だった:

| プロパティ | 値 |
|---|---|
| `display` | `inline` |
| `color` | `rgb(0, 119, 198)`（＝ `--link`。記事内リンクと同じ） |
| `font-weight` / `font-size` | `700` / `13.76px` |
| `background` / `border` / `padding` | 無し / 無し / `0px` |

→ **読者は「これを押すとAmazonに出る」と分からない。**

### 配置（324本すべてを原稿から集計）

| | |
|---|---|
| 総リンク数 | **324** |
| 表の中 | **322（99%）** |
| 表の外（本文中） | **2（1%）** |
| リンクを含むセル | 304 |
| セルの表示文字数 | 中央値 **36** / 平均 39 / **最大 130** |
| 60文字を超えるセル | 20（7%） |

### 二重表示

**同じ行に商品名がすでに書いてあるのに、リンク文言でもう一度フルネームを繰り返しているものが 89本（27%）。**

いちばん重いセル（130文字・リンク3本）:

> ダイワ S2000 **ダイワ クールラインα3 S2000をAmazonで見る**／ロゴス ハイパー氷点下クーラーL **ロゴス ハイパー氷点下クーラーLをAmazonで見る**／アイリスオーヤマ HUGEL 20L **アイリスオーヤマ HUGEL 20LをAmazonで見る**

一方、**脚立の記事はすでに良い形になっている**（商品名を繰り返さず、自分の計算値を添えている）:

> **アルインコ 上わく付 踏台 TBF-5をAmazonで見る**（4.5kg）／**長谷川工業 脚立 SWH-12をAmazonで見る**（7.6kg）

→ **良い書き方は既にサイト内にある。** クーラーボックスの記事が古い書き方のまま残っている。

---

## 2. 規約で確定したこと（原文をcurlで取得してgrepした）

出典: `https://affiliate.amazon.co.jp/help/operating/policies`（2026-08-26 取得・HTTP 200）

### 商品画像 → Creators API 経由のみ

> Creators APIまたはデータフィードを利用することにより、乙は、１つまたは複数のAmazonの関連サイトで
> 提供される商品に関する**データ、画像、テキスト**その他の情報およびコンテンツにアクセスすることができます。

> また、**アマゾン・サイト以外のサイトで提供されたいかなるデータ、画像、テキスト**その他の情報または
> コンテンツも、「プロダクト広告コンテンツ」には**含まれません。**

> (h) 乙は、**画像で構成されるプロダクト広告コンテンツを保存またはキャッシュしてはいけません**が、
> 画像で構成されるプロダクト広告コンテンツへのリンクについては最長24時間保存することができます。

### 価格・在庫 → Amazon提供のリンクか Creators API のみ

> 乙のサイトでは、以下の場合に限り、価格および在庫を表示できるものとします。
> **(a)甲が価格および在庫のデータを表示しているリンクを提供する場合、または(b)乙がCreators APIを経由して
> 商品の価格および在庫のデータを取得し**、本ライセンスに定めるCreators APIの使用に関する要件を遵守する場合。

### その Creators API / PA-API が使えるか → **使えない**

アソシエイト・セントラルの Product Advertising API ページ（2026-08-26 実機確認）。
3条件すべてに **❌** が付いていた:

> ❌ 承認されたアソシエイトアカウントを持つ。
> ❌ Amazonアソシエイトプログラム運営契約を遵守する。
> ❌ **PA APIへのアクセスを取得し、PA APIを呼び出す機能を維持するには、過去30日間に条件を満たす売上を10件有する必要があります。**

⚠️ **審査合格に必要な「180日で3件」より厳しい「30日で10件」。** 画像はかなり先の話。
同ページに「PAAPI が Creators API で利用できるようになりました」とあり、入口は Creators API に一本化されている。

### SiteStripe（アソシエイト・ツールバー）

実機で `リンク生成` を押して確認。出るのは **ストアID／追跡番号の選択と、ショートリンク／完全なリンクのコピーだけ。**
**画像リンク・テキスト＋画像・ウィジェットの選択肢は無い。**

---

## 3. 検索リンクの着地の質（初回・1件だけ実測。第4-D節で17語に拡大した）

`https://www.amazon.co.jp/s?k=ダイワ クールラインα3 S1000X&tag=sunpou-22` を実際に開いた結果:

- **42件がヒット。先頭は「クールラインα」（α3ではない別シリーズ）だった。**
- 目的の S1000X は2〜3番目の「**クールラインα3 10L~25L 各種**」というまとめ出品の中にあり、
  **読者が変種を選び直す必要がある。**

⚠️ **1商品でしか確かめていない。** 324本すべてがこうだとは言えない（**未検証**）。
ただし「型番まで入れても先頭に出るとは限らない」ことは確実に起きる。

---

## 4. できること（推奨順）

規約の制約は「画像・価格」だけで、**下の4つはどれも今すぐできる。**

| # | 打ち手 | 触る場所 | 効き方 |
|---|---|---|---|
| **A** | **`.buy` に専用スタイルを付ける** | `styles.css` のみ | いちばん小さく、いちばん確実。いまゼロなので伸びしろが大きい |
| **B** | **リンク文言の二重表示をやめる** | `build.mjs` ＋ 原稿89箇所 | 最大130文字のセルが半分以下になる。表が読めるようになる |
| **C** | **収益記事に自前の「製品カード」を置く** | `build.mjs` ＋ `styles.css` ＋ 原稿 | 表の外に押せる場所を作る。画像なしでも自分の計算値が主役になる |
| **D** | **検索語を調整して着地を良くする** | 原稿の `[[LINK:...]]` | 見た目ではないが「いい感じ」に直結。要・全件確認 |

**A→B の順を勧める。** A はCSSだけで完結し、B は「良い書き方が既に脚立の記事にある」ので手本がある。
C は形が大きく、D は324本の確認が要るので、**A・B の効果を測ってから**が筋。

⚠️ **効果は測れる。** 分母は Cloudflare のページビュー、分子はアソシエイトのクリック数。
ただし **記事がまだ1本もインデックスされていない**ので（`docs/measurement-2026-08-26.md`）、
**いま変えても当分は自分のアクセスしか分母に入らない。** 見た目を直すこと自体は損にならないが、
**「変えたら増えた」と言えるようになるのはインデックス後。**

## 4-D. 検索リンクの着地を実測した（D の一部を実施・2026-08-26）

**126語（延べ324本）のうち、17語を標本にしてAmazonで実際に引いた。** 10ブランドすべてを含む。
判定は「検索結果の上位5件のタイトルに型番が現れる順位」。

| 順位 | 語数 |
|---|---|
| 1位 | **8**（47%） |
| 2〜3位 | 3（18%） |
| 4〜5位 | 3（18%） |
| **5位までに出ない** | **3（18%）** |

→ **上位3位以内に着地するのは65%。残り35%は読者が探し直すことになる。**

⚠️ **判定基準に既知の弱点がある。** 型番の表記ゆれ（`TBF-5` と `TBF5`）を最初の測定では
拾えず、正しい商品が1位でも「無し」と数えていた。**上の35%は過大評価の可能性がある。**
（途中からハイフンと空白を無視する判定に直した。）

### 仮説と検証：「一般名詞が型番を薄めている」

**当たったが、ブランドによって逆に働く。**

| ブランド | 変更前 → 変更後 | 結果 |
|---|---|---|
| 長谷川工業 RHB-12a | 4位 → **1位** | 改善 |
| 長谷川工業 RHB-21a | 2位 → **1位** | 改善 |
| 長谷川工業 SWH-12 | 2位 → **1位** | 改善 |
| 長谷川工業 SWH-09 | 1位 → 1位 | 横ばい |
| 長谷川工業 SRA-5a | 1位 → 1位 | 横ばい |
| ピカ SEC-S120 | 5位 → **1位** | 改善（変更前は**1位が長谷川の別製品**） |
| ピカ SEC-S210 | 5位 → **1位** | 改善（同上） |
| **アルインコ CCA-80K** | 1位 → **3位** | **悪化**（1位がフィットネスバイクになった） |
| **アルインコ TBF-5** | 1位 → 判定不能 | **悪化**（1位は正しい商品だが表記ゆれ） |
| **アルインコ BSA-240A** | 出ない → 出ない | 変わらず（1位はフィットネスバイク） |

⚠️ **一律のルールは使えない。** アルインコは脚立以外（フィットネス機器）も売っているため、
一般名詞を外すとそちらに流れる。**ブランドごとに測るしかない。**

### 適用したもの（すべて個別に実測済み）

**長谷川工業とピカの3系列・16語・延べ44本**だけを書き換えた。系列内の残りの語も
1つずつ引いて **8/8 が1位** であることを確認してから適用している。

| 変更前 | 変更後 | 本数 |
|---|---|---|
| `長谷川工業 脚立 RHB-09a/12a/15a/18a/21a` | `長谷川工業 RHB-*` | 26 |
| `長谷川工業 はしご兼用脚立 RHB-18a` | `長谷川工業 RHB-18a` | 1 |
| `長谷川工業 強力型脚立 SWH-09/12` | `長谷川工業 SWH-*` | 9 |
| `ピカ 脚立 SEC-S90〜270` | `ピカ SEC-S*` | 7 |
| `ピカ 専用脚立 SEC-S180` | `ピカ SEC-S180` | 1 |

**触っていないもの**：アルインコ全語（悪化するため）／コールマン／キャプテンスタッグ／
未検証の系列（RYZB・RZS・EFA・SE・MCX・SXJ・SCL・上わく付き踏台）／
`ピカ 専用脚立 SEC-S`（型番が付かない系列名なので対象外）。

### ついでに見つかったこと

- ⚠️ **同じ商品を別々の検索語でリンクしている。**
  `アルインコ 専用脚立 BSA-150A` と `アルインコ 脚立 BSA-150A`、
  `ピカ 専用脚立 SEC-S180` と `ピカ 脚立 SEC-S180`、
  `長谷川工業 はしご兼用脚立 RHB-18a` と `長谷川工業 脚立 RHB-18a` が併存していた
  （ピカと長谷川の分は今回そろえた。**アルインコの分は残っている**）。
- **`キャプテンスタッグ CS＋コールドクーラーボックス20` は、どの言い換えでもAmazonの上位5件に出ない。**
  `キャプテンスタッグ UE-113`（型番）でも `CS＋コールド 20L` でも出ない。
  **商品名は正しい。** メーカー公式で確認した → 「**CS＋コールドクーラーボックス20（ブラック）**」
  （`https://www.captainstag.net/products/UE-113.html`・2026-08-26 取得・HTTP 200）。
  **Amazonでの取り扱いが無いか、見つけにくいだけ**とみられる。**書き換えても直らないので触っていない。**

  ⚠️ **この項目は最初「商品名が怪しい」と書いたが誤りだった。**
  端末の文字コード（cp932）で `CS＋` が `CS白` に化けた文字列のままAmazonを引いていた。
  **測定に使った23語を原稿と1件ずつ照合したところ、化けていたのはこの1語だけ**で、
  他22語の測定結果は有効。**bashの出力に出た日本語をそのままクエリに使わないこと。**
- ⚠️ **コールマンは QT 番号が親出品にまとめられていて、どの言い換えでも型番が上位に出ない。**
  `コールマン 28QT` は1位になるが**別シリーズ（ホイールクーラー 28QTBK）**なので採用しなかった。

## 4-C. 製品カードを入れた（C を実施・2026-08-26）

**早見表の外に「決めた人が押す場所」を作った。** リンクの99%が表のセルの中にあり、
表は一覧するための形で押す形ではなかった。

| | |
|---|---|
| 記法 | ```` ```card ```` フェンス（`product` / `lead` / 数字の行）。`site/README.md` に書式 |
| 置いた数 | **7枚**（クーラーボックスの収益記事3・脚立の収益記事3・土鍋の記事1） |
| 中身 | 商品名／1行の説明／**自前の計算値**／ボタン／出どころの注記 |
| 画像・価格 | **無し**（規約上 Creators API が要る） |
| コントラスト | 説明文 5.6:1／数字 11.68:1／ボタン 7.11:1（**すべて AA 以上**） |

⚠️ **`--muted`（#6f7680）をカード背景（`--surface`）に載せると 4.24:1 で AA に届かなかった。**
カード用に `--muted-on-surface`（#5c636e・5.6:1）を足した。

### ★ カードの数字はビルド時に検査する

**カードに書いた数値が、その商品の表の行に無ければビルドが落ちる**（`assertCardNumbers`）。
このサイトは「メーカー公表値から計算する」ことが売りなので、カードにだけ古い数字が残る事故を
指示ではなく仕組みで止める。**わざと 350mm → 351mm にしてビルドが落ちることを確認した。**

```
Error: coolerbox-erabikata.md: カードの数字が表にありません
 → コールマン エクストリームクーラー 28QT の「内寸の深さ: 351mm」の 351
 / 対処: 表の値と合わせるか、表のほうを直す
```

### ついでに直したこと：リンク位置の20%ルール

`HANDOFF.md` は「**全記事で、最初のリンクか収益記事への導線が本文の20%以内**」と書いていたが、
**これは誤りだった。** 本文の描画テキストで測ると `/cassette-konro/donabe-nangou/` だけ **33.5%**。

→ 9号土鍋の表の直後にカードを置いて **20.4%** まで下げた。
⚠️ **まだ 0.4ポイント超えている。** カード自体（説明＋商品名＋数字）がリンクの前に来るため。
他の17本は 19.3% 以下。

## 4-E. モバイル（375px）を実測した（2026-08-26）

**これまで3回「未測定」と書いてきた項目。** ようやく測れた。

**測り方**：`resize_window` はウィンドウを変えるだけで viewport が変わらなかった（375×812 を指定しても
`innerWidth` は 1920 のまま）。**幅375pxの iframe を作り、その中の `contentDocument` を測った。**
メディアクエリは iframe の幅で評価されるので、これで実機相当の判定になる。

| 見たもの | 結果 |
|---|---|
| ページの横スクロール | **なし**（4ページとも `scrollWidth` = `clientWidth` = 360） |
| 表が親からはみ出す | **0件**（全部 `.table-wrap` の中に収まっている） |
| カードが親からはみ出す | **0件** |
| タップ目標（表内ボタン） | **25 × 266〜451px** → **WCAG 2.5.8（AA・24×24px）を満たす** |
| タップ目標（カード内ボタン） | 38 × 159px |
| 同じセル内で隣り合うボタンの距離 | **75px**（誤タップの心配は無い） |

### ⚠️ 見つかった問題：収益記事2本だけ、表の中のボタンが画面外

375px で、表の中の**最初のボタンが左端から何px の位置にあるか**を18記事すべてで測った。

| 記事 | 最初のボタンの位置 | 375px で見えるか |
|---|---|---|
| `/kyatatsu/erabikata/` | **596px** | **⚠️ 画面外** |
| `/coolerbox/erabikata/` | **436px** | **⚠️ 画面外** |
| 他16記事 | 13〜308px | 見える |

→ **収益記事2本では、読者が表を横に910px スクロールしないとボタンが現れない。**
先に来る列（やりたいこと／届きたい高さ／必要な足場）が幅を食っているためで、
リンク文言を短くしても解消しない。

**いまは 4-C で入れたカードが、表の外の押せる場所として効いている**（両記事に3枚ずつ）。
ただしカードは記事の中ほどにあり、**冒頭の「30秒で決めるなら」の表には押せる場所が画面内に無い。**

### ★ 決定（2026-08-26）：直さない

候補は3つあった。**3（そのまま）を採った。**

| # | 候補 | 採らなかった理由 |
|---|---|---|
| 1 | 「30秒で決めるなら」の直後にカードを1枚置く | **どれを1枚選ぶかが恣意的になる。** このサイトは「決めるのは読者、根拠を出すのが運営」という立て方をしていて、「おすすめ」を出すと声が変わる |
| 2 | 表の列順を変えて商品列を左に出す | **「入れたいものから引く」という記事の構造が崩れる** |
| **3** | **そのままにする** | **採用。** カードが表の外の押せる場所を担っている。**未インデックスで効果を測れないうちに、語り口を変えるリスクを先に取る意味が薄い** |

⚠️ **インデックスされて流入が出たら、測ってから再検討する。**

### 残り110語を測るかどうか

⚠️ **測るなら利用者の判断が要る。** 126語すべてを引くには本人のログイン済みアカウントから
100回以上の自動リクエストをAmazonに出すことになる。**アソシエイトは審査中で、却下されたら
再審査がない。** 不自然な挙動と見なされる可能性を考えると、勝手に走らせるべきではない。
今回は**約40クエリ**に留めた。

## 5. やっていないこと・未検証

- ~~モバイルでの見え方を測っていない~~ → **第4-E節で実測した（2026-08-26）。**
  ただし**375px の1幅しか見ていない**。360px・414px・タブレット幅は未測定
- **実機のスマートフォンでは確認していない。** iframe による幅の再現で、
  タッチ操作・慣性スクロール・OSのフォント差は見ていない
- **検索語126語のうち110語は未測定**（第4-D節）。測るならAmazonへの自動リクエストが100回以上要る
- どのデザインがクリック率を上げるかは**このサイトでは未測定**。A/B を回せる流入がまだ無い
